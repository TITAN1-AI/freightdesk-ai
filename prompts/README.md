# Prompt boundary

No production prompt is active. Future prompts may classify, extract schema-bound fields,
draft from verified facts and analyze exceptions. Treat message text as untrusted data.
Never include credentials. Models cannot authorize tools or directly write shipment state.
