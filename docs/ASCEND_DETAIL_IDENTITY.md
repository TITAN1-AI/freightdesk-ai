# Load 1755 identity-only diagnostic

Diagnostic owner-multi-system-ascend-diagnostic-20260911-02 proved normalized Active Loads scope:
12 rows, exact 11 approved IDs, 8 pickups/3 deliveries, no missing/unexpected IDs. It then stopped on
1755 at detail_identity_verification with detail_load_identity_unverified and zero completed details.
Normalization is not revisited or changed. The existing verifier scans visible labeled controls/DTs,
requires a unique exact load-number value, and does not inspect hidden fields, headings or routes.
The actual missing representation is not captured in the prior artifact and is not guessed here.

## New owner command

Attempt: owner-ascend-detail-identity-1755-20260911-01 (prepared only).

```powershell
.\.tools\python\python.exe -m scripts.ascend_detail_identity --owner-authorized
```

Uses the same existing local Python Playwright/Edge msedge profile, same-process owner login, normal
networking and owner-attested Booking Logistics tenant. Reuses unchanged board normalization, checks
exact 8/3 plus all approved IDs, then binds the existing fixed read executor to 1755 only. At most one
detail open; no carrier/driver/stop extraction, other load reads or multi-system phase advancement.

Before click, persist opener tag/link-button presence, ID/name presence, data-attribute count and safe
same-origin path. Never persist query strings, raw hrefs, arbitrary attribute values or unrelated paths.
The existing destination/action guards run before the metadata checkpoint and click.

## Observed identity evidence

Inspect all same-origin frames with a 500-candidate-element bound per frame and 3-second evaluation
timeout. Poll for at most 10 seconds; require repeated stable observations before success. Inspect only
load-ID labels/inputs, semantic headings/tabs/dialog labels/breadcrumbs, named hidden identity inputs,
data-load-id/data-booking-load-id and path-based identity. Board tables/grids are excluded as detail proof.
Only approved Booking IDs may be returned; unknown identity values become flags and cause a stop.
Selectors are positional observed DOM paths, not invented vendor identifiers. Unrecognized query/route
semantics remain unresolved; query strings are never persisted or used to infer identity.

Supported path identity requires an observed /loads/<id>, /load/<id> or /booking-loads/<id> path that
matches the exact 1755 row's pre-click destination. A route without that navigation binding is ambiguous.
Unrecognized/private path segments are redacted. Titles are preserved only for the fixed Ascend app
title; private/dynamic titles are redacted. Customer/lane/carrier/driver similarity is never identity proof.

All candidate IDs must agree with expected 1755. Conflicts, unapproved values, missing identity or
duplicate equally ranked primary sources stop. One visible field takes precedence over a named input,
heading, data attribute or row-linked route. Matching lower-priority signals are corroboration, not
additional independent identities. No duplicate primary selector is selected arbitrarily.

When uniquely proven, persist AscendLoadDetailIdentityContract v1: provider AscendTMS, observed_load
1755, timestamp, evidence source, observed selector/strategy, confidence VERIFIED, supporting count.
Scope is this 1755 observation only; automatic_reader_activation=false. Future contract adoption must
still compare expected load ID to provider-observed ID; fixture strategies are not live provider proof.

## Isolation and output

Separate C:\FreightDeskRuntime\Data\booking-logistics\ascend\identity-diagnostics.sqlite3:
identity_grant, identity_checkpoint, identity_contract. No old attempt or phase records are modified.
Checkpoints contain safe navigation metadata, current path/title, identity labels, element types,
attribute presence, candidate count/approved values, decision and scoped contract. No raw HTML, notes,
phones, rates, cookies, credentials, customer values or exception payloads are persisted/printed.
No production mutations, Graph/CarrierView networking or detail-field extraction is invoked.

Stop on board/session/policy/control failure, origin mismatch, bounds/timeouts, conflicting/missing/
ambiguous identity or persistence failure. No automatic retry. Existing detail-reader identity gate
remains unchanged until actual live evidence supports a scoped contract update.

## Offline validation

Targeted identity and existing field-discovery suite: 21 passed. Final identity-fixture rerun: 11 passed;
additional unstable-observation timeout regression: 1 passed. Ruff and CLI help check passed.
Two existing dependency deprecation warnings remain. Browser tests used intercepted synthetic fixtures
only, never the live Ascend profile. No live attempt was executed and no LIVE_VALIDATED claim added.
