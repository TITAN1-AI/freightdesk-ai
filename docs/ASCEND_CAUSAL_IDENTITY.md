# Load 1755 causal identity diagnostic

SUPERSEDED: this attempt is consumed and failed with a non-specific causal_scan_bound before opener
metadata was persisted. Live execution is now disabled; do not run the historical command below.
See ASCEND_TARGETED_IDENTITY.md for the targeted offline refactor and mandatory one-final-test pivot.

The owner-executed identity attempt owner-ascend-detail-identity-1755-20260911-01 is consumed.
Its local ledger confirms exact scope reconciliation, zero candidate identity fields,
STOPPED_IDENTITY_DIAGNOSTIC / detail_identity_missing and no identity contract. Owner reports an
unchanged /loads route after opening. A same-page panel/modal is a hypothesis, not an observed contract.
The ledger contains both /loads and redacted-path observations; it does not establish an unchanging
path throughout the run. Do not reinterpret a redacted path or infer a panel from missing fields.

## Owner-executed command — prepared only

Attempt: `owner-ascend-causal-identity-1755-20260911-01`.

Run from the FreightDesk AI project in PowerShell:

```powershell
.\.tools\python\python.exe -m scripts.ascend_causal_identity --owner-authorized
```

Uses existing local Python Playwright / Edge channel msedge and the persistent profile
`C:\FreightDeskRuntime\Browser\booking-logistics\ascend`. Same-process owner login/confirmation,
normal application networking, verified origin https://ascendtms.com, tenant Booking Logistics
OWNER_ATTESTED. No Computer Use, alternate browser, new profile or request/body instrumentation.

Reuses unchanged Active Loads normalization and requires the existing exact approved eleven IDs,
8 pickups/3 deliveries for 2026-09-11. At most one detail open, for 1755 only. The diagnostic also binds
the selected opener and exact matching text to the same row's uniquely labeled Load ID column.
Existing action, destination, pause, policy and authenticated-session guards remain unchanged.
Javascript destinations and unsafe operational controls remain blocked even when metadata is recorded.

## Structural evidence

Before the click, capture the opener tag, href type (normal_same_origin/javascript/hash/empty/other),
allowlisted same-origin path, sanitized data-attribute names, approved-ID values, ID/name/class metadata,
onclick presence and safe single-call identity metadata. Only a full fixed read-named call with one
literal numeric argument qualifies as event identity; it is parsed, never evaluated by the observer.
Arbitrary scripts, query strings, fragments, tokens and private attribute values never leave the DOM.
Unrecognized names/classes/paths are redacted. Generic data IDs are metadata, not assumed load semantics.

Snapshot likely dialogs, modal/side panels, forms, detail sections, headings and tabs immediately before
opening. Observe new/visible/changed containers after the click. Fingerprints contain element counts,
control types, approved identity signals, sanitized attributes and relationships; they never hash or
persist raw private text/field values. Only changes observable in this safe structural projection count.
Parent forms containing modal surfaces cannot collapse multiple independent modals into one candidate.

Limits: eight same-origin frames, 80 container candidates/frame, 2,000 descendants/container and
three seconds per frame evaluation. Post-click sampling has a ten-second total deadline, including
session guards and scans. Success needs repeated identical structural samples spanning at least one
second. A frame/context replacement or timeout stops; no reload/retry is attempted.

Relationships are resolved inside the DOM through aria-controls/aria-owns, hash targets,
data-target/data-bs-target or panel aria-labelledby. Raw target IDs stay private; observed positional
selectors and per-observation node aliases describe both ends. No page globals/DOM attributes are
inserted. These aliases are observation references, not reusable provider identifiers.

## Verification rules

All strategies require the exact 1755 data-row/opener binding and one uniquely changed visible detail
surface. Matching nested form/heading evidence may corroborate that surface; independent surfaces stop.

- PROVIDER_DETAIL_FIELD: the resulting surface explicitly exposes the expected load ID in an identity
  field, hidden input, load-specific attribute/identifier or semantic load heading/ARIA label.
- PROVIDER_OPENER_IDENTITY: a load-specific opener attribute or narrowly parsed read-event argument
  explicitly equals 1755, and provider DOM attributes link that opener/row to the unique changed panel.
- PROVIDER_SELECTED_ROW_BINDING: the exact row/control becomes selected or expanded, and provider DOM
  attributes link it to the unique changed panel. Prior selection alone is insufficient.

A click, customer/lane similarity, generic data value or lone panel appearance is insufficient.
Any conflicting approved ID or nonempty unapproved identity value stops with CONFLICT. Multiple
independent changed surfaces stop with AMBIGUOUS_DETAIL_CONTAINER. Missing provider binding stays UNKNOWN.
Unstable DOM, changed opener, missing row/header binding and existing read/session/policy gates stop.

On success, persist AscendLoadDetailIdentityContract v1 with provider AscendTMS, target 1755, timestamp,
confidence VERIFIED, strategy/relationship and evidence categories. Scope is the observed 1755 surface;
automatic_reader_activation=false and write_activation=false. The existing operational identity gate
is untouched. Future reader adoption must still compare expected_load_id == provider_observed_load_id.

## Isolation

Runtime-only `C:\FreightDeskRuntime\Data\booking-logistics\ascend\causal-identity-diagnostics.sqlite3`
stores one-use causal_grant, append-only causal_checkpoint records and an optional causal_identity_contract.
Old identity/multi-system ledgers remain untouched. CLI output is a compact safe status; structural
evidence stays in this isolated database. No carrier/driver/stop/rate extraction, canonical mutation,
operational communications, CarrierView/Graph phase or production write executor is invoked.
No live execution or capability promotion occurs during offline preparation.

## Offline checks

25 targeted tests passed across causal identity, prior identity diagnostics and existing read-control
field discovery. Final causal rerun: three tests passed, including 16 synthetic browser scenarios,
one-use/no-operational-extraction isolation and unstable-DOM rejection. Ruff and CLI help passed.
Two existing dependency deprecation warnings remain. All browser fixtures used intercepted requests
and test-only profiles under C:\FreightDeskRuntime\Data\TestRuns; the live profile was not launched.
The new runtime diagnostic database is absent at handoff; no new grant was consumed.
