# GitHub collaboration decision - 2026-09-13

Owner requested a shared repository for agentic coding. TITAN1-AI/freightdesk-ai was created PRIVATE;
the owner subsequently changed it to PUBLIC on 2026-09-13. Preserve this owner-selected visibility,
audited history, main plus scoped branches/PRs, and synthetic-only Windows CI.
Pin CI actions and give its token contents:read only; publish no runtime artifacts or provider secrets.
Repository access does not authorize live provider work. Contributor invitations and any future
visibility change require an owner decision. No automatic merge or production deployment is enabled.

# Decisions - latest 2026-09-13

## Repair audited boundaries without live deployment

Use A01-A10 as the defect list. One observation fence and field-node links must make legacy metadata
and graph coherent before the runtime-owned atomic commit. Compatibility3/graph2/wire1 retains
message limits. Keep document/realm node identity independently of observation handles. Admit static
label text before access; never use arbitrary neighbors/aggregate text. Field/domain meaning belongs
to the application. History uses additive indexes and bounded latest/session queries, without rewrites.
Regressions exercise the actual facade, host and synthetic full chain, including real process exit
following runtime commit and recovery without read replay. Local baseline6d4a781 and source-hash/
rollback rehearsal are source-only. No installed-state or vendor change; V1 default, V2 TestRuns-only
and OBSERVE-only remain. See docs/WEBBRIDGE_V2_AUDIT_FIXES.md.

## Historical decisions

# Decisions

## 2026-09-12: accept V2 Phases 0–2 through the offline X1 compatibility chain

Preserve existing proof, metadata collector, message limits and runtime transaction ownership.
Add only an explicitly installed TestRuns adapter; no live flag, new transport or permissions.
Graph and V1 section proofs must select the same region. Host recomputes relationships. The digest
covers both accepted representations; map/evidence/completion commit together. Coordinator recovery
references the existing receipt. OBSERVE only, with no automatic approval migration. Partial-stage
DTOs explicitly defer field/table/navigation/value domains. Narrow Playwright adaptations carry exact
pin/hashes and Apache LICENSE/NOTICE. See docs/WEBBRIDGE_V2_PHASES_0_2_ACCEPTANCE.md. No live grant
is reopened and no synthetic test promotes an Ascend capability.

## 2026-09-12: begin V2 offline foundation; distinguish it from integrated acceptance

Owner approval starts Phases 0–2 offline only. Keep draft graph code unregistered in the live X1
manifest and use TestRuns-only storage during isolated testing. Snapshot scalar boundary values
before acquisition so mutable host state cannot rewrite both sides of a consistency check.
The revised brief comparison identifies the remaining compatibility, typed-evidence and owning-store
integration requirements. Do not count graph-only persistence or 61 focused passes as completed
Phases 0–2. See docs/WEBBRIDGE_V2_PHASES_0_2_REVIEW.md. No live authorization is reopened.

## 2026-09-12: propose WebBridge V2 from pinned source research; preserve X1 boundary

Study individual algorithms rather than adopt a browser framework. Proposed V2 has one immutable
DocumentGraph with Control/Schema views, document-scoped node identities, typed relationships,
bounded locator portfolios, metadata-only mutation/state evidence, claim-specific verification
and an atomic map/evidence receipt. Ascend meanings and acceptance stay in the provider adapter;
the existing context assembler still requires separate trusted field-read validation.

Borrow permissive indexing/semantic/selector primitives only with provenance and required notices.
Keep ambiguous identity, policy and causality outside numeric confidence scoring. No raw snapshots,
private-value hashes, automatic healing, new browser sessions or widened permissions. The roadmap
starts with offline invariants and graph/relationship phases after owner approval. This decision
does not authorize implementation or live execution and makes no new LIVE_VALIDATED claim. See
[the source harvest](docs/BROWSER_INTELLIGENCE_CODE_HARVEST.md) and
[the architecture](docs/WEBBRIDGE_V2_ARCHITECTURE.md).

## 2026-09-12: close successful five-iteration X1 scope and retain candidate-only mapping

The agent completed exactly five bounded iterations (including local reconnect preflight).
Final live evidence passes fresh session/readiness/dispatch/ACK, exact provider LOAD/1763 and
one Load Basics metadata map with 19 candidate fields. Revoke job-owned authority immediately;
preserve the map and audit at OWNER_REVIEW_REQUIRED without approving or traversing navigation.
The current authorization is exhausted and complete. Further provider reads need new scope.

Use the observed unique selected current-route provider control and agreeing visible heading,
then a bounded unique later form branch in the nearest form-bearing heading context. The
captured region must exclude identity/navigation/other section headings. Reject ambiguity and
preserve all existing field/write/session/presence limits. This structural rule is evidence-backed;
no Load Basics selector or requested-name oracle is introduced. Mapper module revision 2 is
handshaken separately from public 0.6.2 and trace revision 1. Candidate semantics stay unvalidated.

Only exact A/B/C capabilities are promoted from real receipts. Broader OBSERVE, AUTO_MAP,
operational fields and other loads/sections remain unvalidated. The original DOCUMENT_CHANGED
branch stays UNKNOWN because it was not recorded and did not recur. Final 411-test relevant
suite and scoped Ruff passed. See docs/ASCEND_X1_SESSION_CAUSAL_DEBUG.md.

## 2026-09-12: preserve native acknowledgement bindings and prove document focus

The owner's direct chat authorization resolved the earlier approval block. Count the local
reconnect preflight stop conservatively as iteration 1 of five. The new causal ACK omitted the
binding fields required on every authenticated body; add them at the host, retaining the worker's
checks. An actual-background regression reproduces the old rejection and corrected acceptance
with a revoked lease. Never repair this by resetting enrollment or relaxing ACK validation.

Browser foreground alone did not prove page focus: tab selection left focus in chrome. The local
helper now requests page focus via the browser's F6 control; X1 still independently requires
document visibility/focus. Iteration 2 passed Phase A and exact target identity, then stopped on
unverified section structure. Investigate only sanitized relationships within that verified
workspace; do not infer the section from the requested Load Basics name.

## 2026-09-12: diagnose the actual queued-capture invalidation before section work

Preserve DOCUMENT_CHANGED and other safe runtime errors instead of rewriting queued-capture
failures as SESSION_UNVERIFIED. Keep the existing stop/revoke gates. Add an explicit diagnostic
opt-in, fixed reason/event schemas, hashed binding identifiers and 256-event job budget that cannot
block revocation. Actual worker/content trace revision is proved through the private content/native
channel, not source-file versions alone. Normal mapping authority and permissions are unchanged.

The owner's new attachment requests an agent-run loop up to five materially distinct iterations.
Automatic approval review nevertheless rejected reload as conflicting with older instructions.
Request direct confirmation and preserve the block; do not use another execution path to bypass it.

## 2026-09-12: X1 Mapping Reliability Fix Pack

Implement the owner's requested review findings as one scoped offline pack (X1 0.6.2). Readiness
is a fresh private document visibility/focus receipt, separate from provider workspace/section proof.
Local committed job changes wake the existing authenticated native pipe through an append-only
hint outbox; notifications confer no authority. Cross-load optional fields are candidate variation,
while identity/navigation and same-workspace drift stay fail-closed. Normal owner status exposes
safe stages, predicates and cleanup instead of hiding them in advanced reports.
Lease acquisition, capture queueing and cleanup use transactional authority checks. Paused/resumed
and navigation-review continuations wait for unrelated read jobs without replacing their grants.

Use the actual integrated execution chain with synthetic credentials, Chrome API boundaries and
intercepted provider DOM as the offline acceptance test. Its additional whole-number timestamp MAC
defect is corrected without alternative signature acceptance or protocol-validation weakening.
Do not infer real provider compatibility or LIVE_VALIDATED status from these fixtures. See
[ASCEND_X1_MAPPING_RELIABILITY.md](docs/ASCEND_X1_MAPPING_RELIABILITY.md) for the exact owner procedure.

## 2026-09-12: analyze integration before another Ascend retry

The owner requested a workflow review. Read-only local audit found a newer job that reached
workspace identity proof and failed section recognition in about 2.30 seconds. Preserve this
distinction from the preceding owner-presence rejection and expiry. Do not diagnose login,
transport, field values or a specific live selector without evidence.

Review output is [ASCEND_WORKFLOW_REVIEW.md](docs/ASCEND_WORKFLOW_REVIEW.md), with standalone
synthetic reproductions of observer lifecycle loss, nonrecovering read-job dependency wait and
cross-load optional-field drift. Reproduction success means a defect exists, not product validation.
No production code/gate change, new job, lease, browser or vendor action was performed. Recommended
next engineering work joins owner readiness, actual lifecycle/hint production, coordinator recovery,
safe section diagnostics and persistence in integrated offline tests before another owner live run.

## 2026-09-12: X1 0.6.1 CURRENT_LOAD immediate handoff (offline)

The first live orchestrator CURRENT_LOAD job has zero maps. Local sanitized audit corrects the
coarse expired-job report: mapping was queued at 03:22:55.347215Z, dispatched at 03:23:15.307047Z,
and rejected at 03:23:15.322125Z with MAPPING_OWNER_NOT_PRESENT. Session receipts succeeded.
No evidence establishes a load/DOM incompatibility. The audit does not establish why foreground
presence was absent at execution. The host emitted a dispatch; no useful WebBridge inspection followed.

Two code defects were corrected: probe-to-mapping lease replacement invalidated the port and let
the worker wait for a later wake; CURRENT_LOAD inherited the normal OBSERVE quiet-wait treatment
of owner-presence rejection, hiding it until the job deadline. The worker now rebinds within the
same bounded wake and gives queued capture priority over ordinary cadence. No owner navigation
hint or manual Capture button is needed for CURRENT_LOAD. Orchestrated failures remain explicit.

Persisted capture lifecycle: NOT_REQUESTED / QUEUED / DISPATCHED / ACKNOWLEDGED / COMPLETED,
with requested/dispatched/acknowledged/completed timestamps. DISPATCH is a host event; ACK is a
validated content command receipt; COMPLETED means successful canonical provider-map validation
and local map persistence (no canonical shipment mutation). Neither dispatch nor ACK proves DOM identity.
Session proof is bounded at 30 seconds for CURRENT_LOAD; dispatch at 8 seconds; acknowledgement at 3;
acknowledged capture at 5, within existing 12-second executor authority and 3-second DOM capture budget.
Overall job expiry remains a cleanup fallback. Failed jobs/audit stay intact; no replay/reset.

No live execution/new job/lease occurred here. Owner reloads existing X1 to 0.6.1; no re-pair.
Same one-command retry, no ID or manual lease steps. See docs/ASCEND_MAPPING_ORCHESTRATOR.md.
OBSERVE change-driven continuation and owner-opened next-cohort workspaces are preserved.
No new LIVE_VALIDATED promotion, provider writes, operational extraction or identity-gate relaxation.


## 2026-09-11: Mapping Orchestrator V1 / X1 0.6.0 (offline)

Read docs/ASCEND_MAPPING_ORCHESTRATOR.md. A single protected-owner Map Ascend action or
scripts.ascend_mapping_orchestrator start manages durable stages, IDs, session-only preflight,
bounded mapping authority, automatic starting capture, navigation review, qualified AUTO_MAP,
reports and cleanup. A repeated click preserves the active job. Interrupted capture never retries.
The existing extension/native transport, provider workspace contracts and WebBridge sensors are reused.
Planning/accounting operations scope is still unverified and fails closed; Active Loads remains
VISIBLE_BOARD_ONLY. Representative cohort loads are owner-opened; reviewed sections traverse automatically.
General AUTO_MAP still requires controlled cohort return receipts. Read/write field mappings remain
unvalidated. No new LIVE_VALIDATED promotion and no live job/lease/attempt was created this turn.
The old manual -02 ID and all prior audit remain intact. The orchestrator supersedes manual per-step
lease/session/capture/report commands as the normal owner workflow; advanced diagnostics retain IDs.
Owner first reloads existing X1 to 0.6.0, opens 1763 / Load Basics, then runs the single documented start
command. No re-pair, Computer Use, Playwright live run, vendor request or production write here.


## 2026-09-11: X1 0.5.1 OBSERVE capture diagnostics (offline)

Owner session owner-x1-mapping-20260911-01 persisted zero provider maps. Local audit shows
session VERIFIED at 2026-09-12T02:08:41.468188Z, mapping dispatch at 02:08:41.491432Z,
and READ_TIMEOUT recorded at 02:11:48.689305Z (187.198 seconds later). The configured
read deadline was 12 seconds; the audit gap is not proof of DOM execution duration.
No internal capture stage/count was persisted, so the precise live timeout cause remains UNKNOWN.
Code inspection found page-wide visibility checks before bounds and repeated workspace discovery.
0.5.1 filters semantic candidates before layout checks, uses a 3-second cooperative capture deadline,
revalidates within the discovered shell, and records bounded metadata-only stage receipts through
the existing paired channel. Renderer/process suspension can still exceed wall-clock deadlines;
the host retains the last received stage and rejects late maps. No timeout increase or transport redesign.

New unused prepared session: owner-x1-mapping-20260911-02. OBSERVE only; cohort
1763,1755,1769,1737; first retry is enforced as 1763 / Load Basics, one capture maximum.
No session enabled, installed extension reloaded, vendor execution or live promotion here.
Old audit is preserved. Read docs/ASCEND_X1_OBSERVE_RETRY.md before owner execution.
Operational detail facts remain UNKNOWN; maps CANDIDATE_ONLY and metadata only.


## 2026-09-11: X1 0.5.0 OBSERVE + AUTO_MAP workspace architecture (offline)

Read docs/ASCEND_X1_MAPPING_MODE.md. OBSERVE learns unknown provider structure and drift through
owner-visited sections. AUTO_MAP traverses VERIFIED READ_ONLY_NAVIGATION controls within one exact
provider-proved LOAD workspace, rechecking session/lease/identity/control before each transition,
verifying the expected section afterward, and returning through a verified starting-section control.
No repeated manual tab visits are required once navigation is verified. No arbitrary selector/script,
write control, other-load opener or crawler is accepted. Known same-document role=tab controls only;
unobserved navigation/operational fields remain UNKNOWN. Tenant identity remains OWNER_ATTESTED.
The 3-5 load cohort is only controlled AUTO_MAP validation, with OBSERVE preparation for unknown UI.
Normal owner-present modes need no mandatory ID list; optional tighter scope remains. Normal AUTO_MAP
requires reviewed navigation and owner-confirmed successful cohort return cycles; OBSERVE evidence
alone cannot unlock it. Time/workspace/section/field/read-attempt bounds remain. Metadata contracts are
append-only candidates; controlled navigation approval does not activate field reads or writes.
Source0.5.0 preserves existing enrollment/transport/permissions, Active Loads and VISIBLE_BOARD_ONLY.
No live execution, lease mutation, registry change or new LIVE_VALIDATED promotion. Old proposed panel
-detail -03 is superseded. The handoff includes exact future owner commands and validation limits.
220 targeted offline cases passed; final affected reruns passed (latest: 32 cases). Ruff and package
JavaScript syntax passed; the two existing dependency deprecations remain.
The 5-10-second complete context target remains unproven; operational reads/context latency need
separate validation after the navigation cohort. Current context interface preserves unknown facts.

## 2026-09-11: bound causally relevant detail roots, retain exact failure metadata

Do not increase the generic12-container budget to accommodate unrelated Ascend layout. Source0.4.3
uses lightweight baseline membership/visibility, followed by direct/new/newly-visible/connected
selection graph edges and category bounds. Unique strongest causal relevance chooses a root; it does
not replace provider identity. Equal top scores or conflicting identity stop. No unrestricted DOM
walk, unknown selector fallback or automatic live retry. Metadata-only mappings remain candidates.
Persist stage/click/category/count/max/rank evidence under the exact consumed attempt so later global
state cannot obscure it. Historical -02 lacks those measurements; preserve unknowns. No change to
healthy prerequisite systems, board schema, enrollment, transport, permissions or write authority.
See docs/ASCEND_X1_CAUSAL_CONTAINERS.md for evidence, offline tests and proposed owner-only -03.

## 2026-09-11: diagnose schema failures structurally without weakening schema proof

Do not infer the exact current Ascend DOM from BOARD_SCHEMA_INVALID or retained board evidence.
The failed live receipt did not preserve its schema predicate or counts. Correct the independently
reproduced clone competition and positional/case-sensitive matching in source 0.4.2, retaining the
33-column/31-named-header contract, exact ID/pick/drop meanings, span and ambiguity guards.
Two consistent structural samples within three seconds permit bounded rendering; independent view,
lease and writes gates remain unchanged. Missing/ambiguous schemas still stop. Empty tbody alone
is insufficient. Structural receipts export canonical header labels/UNKNOWN_LABEL and counts only;
safe predicates propagate to audit, CLI and UI. Old missing evidence is never invented/backfilled.
No additional permission, transport, enrollment, live run or capability promotion. Owner's revoked
lease requires a future explicit short renewal after reload; no re-pair or detail attempt.
See docs/ASCEND_X1_BOARD_SCHEMA.md for live evidence versus offline reproductions.

## 2026-09-11: source X1 0.4.1 lifecycle correction (offline only)

Local evidence shows a fresh 8-row Active Loads receipt at 22:04:35 UTC, then CONTENT_SCRIPT_STALE
at 22:04:40. Current count was retained after failure. Initial handshake category/builds were not
persisted. Deterministic defect: stored REFRESH_ASCEND_TAB prevented subsequent tab handshakes even
after manual refresh/login. Do not tell the owner to refresh/re-pair again.

Source0.4.1 adds a manifest/worker/content/host/controller build handshake, document generation,
fixed packaged top-frame ISOLATED reinjection and precise missing/old/port/document/protocol/policy
states. The scripting API capability is newly declared; host permissions remain Ascend-only.
Versioned dispatch prevents old surviving listeners from executing reads. Current vs retained board
evidence is explicit. Source update preserves durable enrollment; expired leases alone need renewal.
See docs/ASCEND_X1_CONTENT_LIFECYCLE.md for evidence, limits, permission change and owner retry.
260 targeted offline tests passed; Ruff and JS syntax passed. At 22:24:20 UTC final local check,
the existing lease had expired at 22:19:34 UTC (not revoked); the next owner retry needs renewal.
No live run, lease enable, re-enrollment or detail attempt was executed. Board LIVE_VALIDATED scope
is preserved; this recovery and all operational detail mappings remain not live validated.


## 2026-09-11: X1 operational detail contract v1 prepared offline (source 0.4.0)

Added bounded WebBridge DOMSnapshot/DOMDiff, BrowserSensor, LocatorGraph, AdaptiveLocator,
ProviderContract and EvidenceScorer components to X1's existing transport. Exact provider detail
identity precedes metadata-only mapping of 19 operational fields. LEVEL_1/2 can verify an observed
read mapping; LEVEL_3 remains PROPOSED and conflicts UNKNOWN. Every contract remains CANDIDATE_ONLY;
no automatic activation, private values, finance/notes, writes or vendor LIVE_VALIDATED promotion.

A new one-use owner detail queue pins one ID from a fresh Active Loads board and its semantic hash,
then performs FIND/OPEN/identity/discovery and stops. Append-only candidate versions, atomic receipt
persistence, safe metadata report and offline drift/identity/security tests are implemented.
Proposed unused ID owner-x1-detail-20260911-01 is not consumed. No live runtime schema change, lease,
read, host execution, extension reload or re-pair was performed. Read access remains revoked.
See docs/ASCEND_X1_DETAIL_CONTRACT.md for exact six-minute owner-lease steps and stop conditions.
244 targeted offline tests passed; Ruff, JavaScript syntax and runtime Node fixtures passed.
Existing dependency deprecation warnings remain. No live execution.

The earlier board runtime remains LIVE_VALIDATED (three cycles, 11 visible loads, one proposal,
two no-ops and no post-revoke board reads). Detail mappings remain UNKNOWN / not LIVE_VALIDATED;
VISIBLE_BOARD_ONLY and OWNER_ATTESTED limitations remain. No all-load detail batch is prepared.


## 2026-09-11: X1 0.3.1 local tab-lifecycle diagnosis (no live execution)

Owner confirms durable enrollment/reconnect worked. Existing runtime receipts prove one eligible
Ascend tab at `/`, two authenticated session reads and successful automatic binding, followed by
ASCEND_NAVIGATE_ACTIVE_LOADS / READ_TIMEOUT at its 12-second deadline. No board receipt/proposal.
The underlying browser timeout cause and tab/reload ordering were not persisted. The observed
15-minute read lease expired at 19:09:30 UTC; no new lease or enrollment was created here.

Source 0.3.1 adds version-aware, lease-gated bounded refresh for an existing stale tab, port reuse,
explicit navigation transition/fresh-document proof, distinct discovery/view/scheduler statuses,
and safe stage/wake/count diagnostics. Extension alarms are the execution owner after CLI exit.
WAITING_FOR_ASCEND now requires a zero-tab observation; initial state is DISCOVERING_ASCEND.
Reload is required to adopt source changes. Existing enrollment is preserved; owner retry needs a
fresh read lease because the inspected lease expired. See docs/ASCEND_X1_TAB_LIFECYCLE.md for the
12-question evidence table, limits, recovery rules and exact owner commands. No automatic live run.

215 targeted offline tests passed; Ruff and JavaScript syntax checks passed. Existing dependency
deprecation warnings remain. No live compatibility promotion for 0.3.1.


## 2026-09-11: persistent enrollment and read leases replace the bootstrap operating model

The owner explicitly authorizes an offline implementation of persistent read-only X1. Keep the existing
Native Messaging architecture and exact extension origin. Initial owner enrollment is a distinct
purpose-marked bootstrap; a successful old pairing cannot silently enroll. Protect durable Windows,
device, host installation, tenant/actor/protocol binding with DPAPI; keep only an opaque handle in Edge.
Use fresh connection HMAC keys and renewable short native sessions without daily bootstrap imports.

Read authority is a separate owner-enabled lease (default8h), bounded by policy/pause/revoke/expiry and
security state on dispatch and receipt. The host owns cadence and replay/proposal history. Automatic
tab routing requires fresh provider authentication; persisted binding hints never establish identity.
Active Loads navigation is typed/fixed and must end with independent provider view proof. Unknown
field contracts remain UNKNOWN, never unlocked by a broader read vocabulary. No writes/canonical apply.

Keep the old consumed ledgers intact and use a separate runtime store for leases, scheduler checkpoints,
safe receipts and UNAPPLIED proposals. Do not force the old 8/3 oracle onto continuous operations. Hash
the verified ID/date projection only and disclose its coverage. Explicit local dashboard actions require
the protected owner session and CSRF header; open demo trust cannot enable runtime reads. No actual
enrollment/lease/vendor execution or LIVE_VALIDATED promotion during implementation. See
[persistent runtime handoff](docs/ASCEND_X1_RUNTIME.md).

## 2026-09-11: establish X1 board view before reading its manifest

Use a versioned provider DOM view contract with strict host re-resolution. Restore parent li.active
and aria-current=true support, and accept deterministic selected controls, primary headings and
explicit board/panel relationships. Return proven other views; fail closed on absent/conflicting
signals or view changes, including changes away and back during a command. Never derive view identity
from /loads, IDs, board membership or 8/3 counts. Persist only allowlisted structural metadata in the
existing signed receipt path. No Native Messaging architecture/permissions/pairing changes.

Separate the confirmed old detector gap from the unrecorded actual live marker: -01's session receipt
is observed, while its board view/extraction remains unknown. Preserve consumed -01 and stopped tab
binding. Prepare an inert, explicitly superseding -02 owner CLI only for this reviewed exact failure;
no actual authorization or retry now, no reset and no general successor loop. Pairing's existing
ten-minute lifetime and Reload invalidation remain; expired pairing must not be called currently valid.
117 targeted fixture tests validate implementation only. No new board/detail LIVE_VALIDATED promotion.
See [view contract and conditional owner handoff](docs/ASCEND_X1_VIEW_CONTRACT.md).

## 2026-09-11: separate proven pairing from host-authorized X1 reads

Accept the owner's X1 0.1.2 PAIRED / PAIRING_SUCCESS report as LIVE_VALIDATED local transport only,
provenance OWNER_REPORTED. Implement the existing extension/native architecture's first read controller
in 0.2.0; no replacement transport or Playwright identity retry. Keep pairing acknowledgement separate
from the host's explicit, short-lived READ_DISPATCH. The host owns local owner grants, read_ascend
policy, pause/takeover, one-use sequence, selected document, board revision and sanitized receipts.
Only four identity-test commands are released, each on an explicit owner button press. No auto retries.

Reuse the operational workflow's explicit date parser for narrow ID/date board facts, then independently
compare membership with the owner-reconciled one-test oracle. Preserve visible-board-only coverage and
derived classifications. A retained exact provider row/opener must connect to deterministic detail
identity by a field, opener identity/direct panel reference, or provider selection/direct panel binding.
Click alone is never evidence. Even a successful identity receipt does not enable operational extraction.
Tenant identity stays OWNER_ATTESTED; no provider-derived company claim. Read-only-ready is an authorization
state, not LIVE_VALIDATED. No live execution/preparation in this coding turn; see docs/ASCEND_X1_READONLY.md.

## Native host startup diagnosis: flush relay, retain registration and pairing gates

Fix the reproduced incremental stdio buffering defect with explicit flush in the existing C# binary
relay. Keep exact origin pinning, Windows/runtime binding, DPAPI, native pipes and all production
blocks. Preserve both valid registry entries; provide owner-only RebuildLauncher without registry
access. Add independent safe launcher/Python startup checkpoints and a one-ping --self-test process
mode that cannot pair or operate vendors and is unreachable through normal native messages.
Owner runs the local self-test after rebuilding, then stops before another bootstrap or pairing.
Do not overstate the old audit: it proves accepted HELLO, not delivery/verification of the challenge.

## 2026-09-11: instrument existing local pairing; no transport pivot

Treat owner-reported native host reachability separately from pairing success. Fix error visibility,
not Ascend/session networking. Use the existing stdio transport for safe pre-pair diagnostic metadata;
accept only fixed codes/stages and booleans. Preserve consumed-request and pairing ledgers. Provide an
explicit Import action and persistent packaged panel to make file-input/restart behavior observable.
Require the first signed acknowledgement's installation/generation as well as protocol/principal/session
binding before PAIRED. Never expose READ_ONLY_READY. No new permissions, registry writes, bootstrap,
browser/vendor execution or architecture. Actual original failure remains unknown pending owner retry.

## 2026-09-11: local native pairing is separate from production read authorization

Implement stdio-only Native Messaging with exact owner-entered extension ID, Windows user/runtime
installation, booking-logistics/FreightDesk-Avery and version binding. No guessed ID or wildcard.
Use an explicit owner-run helper for per-user Edge registration; never modify registry during this task.
Use DPAPI for the host key and a restricted runtime-local, ten-minute one-use bootstrap imported by the
popup. Keep the browser key in worker memory only; require deliberate re-pair after lifecycle loss.
Preserve consumed request/audit ledgers across reset/uninstall; no automatic retries or fallback.

0.1.1 exposes manual pairing/status only, no vendor read dispatch. Reserve READ_ONLY_READY until
policy-bound tab/controller routing and identity-only stabilized receipts are implemented, offline-tested
and owner-authorized. Plan fresh 8/3 board reconciliation and one exact 1755 open, provider identity,
then stop before operational extraction. No LIVE_VALIDATED promotion or reopened Playwright experiment.
See docs/ASCEND_NATIVE_MESSAGING.md for the owner handoff and documented registration model.

## Primary interactive executor candidate: Ascend extension bridge

Honor the final failed Playwright identity attempt: no more detail experiments, retries or grant resets.
Prefer an MV3 extension plus Native Messaging for interactive Customer Zero; preserve Playwright for
proven board execution and future validated background jobs. Native pipes avoid a public/loopback listener;
require private pairing plus pinned extension/host identity. No production connection in X1.
Option B is acceptable for exact board-row facts, insufficient for detail attribution or writes on the
observed evidence. Identity remains provider-backed, all authority stays in FreightDesk. Typed read
commands only; no generic selector/JavaScript interface. See docs/ASCEND_EXECUTOR_PIVOT.md.

## Owner authorizes one final targeted identity attempt

Prepare owner-ascend-final-identity-1755-20260911-01 for owner execution only. Require its exact CLI ID
and use final_causal_grant as a one-use namespace independent of historical consumed causal grants.
Any existing final grant blocks another run, even if an ID were changed. Do not reset it. Failed
final execution triggers the bridge-versus-row-bound engineering assessment; neither alternative
is automatically authorized. Provider identity, policy and write guards remain unchanged.

## Targeted causal diagnostic and end to repeated identity experiments

Stop live execution and preserve the consumed causal grant. Resolve 1755 from Load ID cells only;
persist opener metadata before fingerprinting visible/directly referenced containers. Identity scans
are confined to new/newly visible/referenced surfaces. Replace shared bounds with categorized measurements.
No new ID/command this turn. At most one future owner-authorized targeted test may be considered; any
failure requires an extension-bridge assessment or separately approved row-bound model with provider
evidence. No automatic pivot implementation, identity inference or write authorization.

## 1755-only causal identity evidence; offline preparation

Keep the operational identity gate unchanged. A single clicked row is not provider identity proof.
An observed contract requires a unique changed detail surface and explicit provider detail identity,
or load-specific opener identity plus stable DOM binding, or a newly selected exact row plus stable
DOM binding. Reject conflicting IDs, multiple surfaces and unknown relationships. Version/provenance
are scoped to the observation; no automatic reader or write activation. Separate one-use runtime
diagnostic, no remaining ten loads, no other vendor phase. See docs/ASCEND_CAUSAL_IDENTITY.md.

## 2026-09-11 — Active Loads is the POC #002 operational source

Use Active Loads, not All Loads, to define Avery's operations population. Store immutable, hash-bound
structural observation metadata and require exact owner reconciliation (8 pickups / 3 deliveries) before
any downstream read or proposal. Retain All Loads only as an optional audit comparison; never merge its
extra service-date rows. A changed or unre-read board blocks action.

## Current — approved historical canonical import and display

Owner confirmed CarrierView UI stop windows/arrival/departure and explicitly approved historical
POC import. Persist an immutable evidence hash and owner confirmation, exact canonical identity,
raw provider metadata and source seconds. Historical approval does not need real-time freshness;
the existing real-time import gate remains unchanged.
Unknown customer/driver name/acceptance/documents remain unknown. DELIVERED and arrival/risk
assessments are labeled FreightDesk-derived. POC is paused/human-owned in a separate database,
with no operational tasks. Only proven historical reads, reconciliation, import and display are
LIVE_VALIDATED; all owner-excluded capabilities remain false.
Expose through a private one-use launcher grant and separate HttpOnly session; demo bootstrap
cannot authorize live facts. No provider credential reaches the browser and no provider request
was needed for import. Actual protected browser rendering verified.

## Latest — exact-reference historical replay

Owner selected Booking reference 1752 as completed LIVE POC #001 and authorized read-only replay.
Exactly three tenant GETs were made for its unique provider record: load detail, last position,
positions history. No profile/list repeat, other-shipment read, production write or import occurred.
Actor remains FreightDesk/Avery, credential class tenant.

List/detail stop shapes differed: keyed objects with UTC appointment ends versus a stop array
with local appointment windows. The initial reconciliation guard stopped before position/history;
offline mapping using the listed IANA zone resolved the mismatch. Cached exact detail was reused,
not fetched again. Provider/Booking IDs, driver, integration, addresses, appointment ends and
time zones reconcile. Unknown fields and raw source timestamps remain in nonsynced evidence.

Offline stop-event replay shows pickup and delivery arrival within appointment windows and
delivery arrival/departure flags true. This is historical arrival assessment, not a claim of current
healthy tracking or full lifecycle completion. App status alone does not prove driver acceptance.
Position history returned 10 of 218 reported records (page 1 of 22); no undocumented pagination
request was added. Full GPS replay/order and numeric timestamp units remain unverified.
POD, signed RC and billing readiness remain unknown; document API remains unsupported.

Sanitized replay report: C:\FreightDeskRuntime\Data\booking-logistics\historical-1752\replay-report.json.
Raw evidence stays under the corresponding runtime run directories, never in source/log output.
No existing CarrierView UI tab was available. Independent owner UI comparison remains pending.
No canonical shipment import, operational scheduler activation or shipment LIVE_VALIDATED claim.
142 tests, Ruff and JS rendering passed. This update supersedes prior unselected-candidate status.

## Current service credential and historical discovery

The owner explicitly configured the Booking Logistics tenant token as the CarrierView API service
credential. Avery's employee token is unavailable for API execution; real agent requests and the
agent launcher are blocked. This is configured selection, not fallback. Tenant class remains in audit;
actor is FreightDesk/Avery. Reason: "CarrierView API requires manager/admin credential; Avery employee
token is not API-eligible." This is the owner's eligibility determination after two user_not_found
profile responses; it is not an independently verified statement about all CarrierView employee accounts.

One tenant GET /api/profile succeeded (success=true), with company identity matching Booking Logistics LLC.
One conditional GET /api/loads?filter=past succeeded and returned 32 candidate records.
Stopped before exact-load reads, replay actions or import. No production writes, messages or webhook calls.
Candidate references only were reported; private source evidence remains under C:\FreightDeskRuntime.
Multiple candidates (including a duplicate Booking reference) require owner shipment reconciliation.
Profile and past-list connectivity succeeded; shipment/workflow LIVE_VALIDATED remains false.
Owner must identify the known completed shipment before reconciliation continues.
138 tests passed, plus Ruff and JS renderer; no automatic network retries occurred.
This section supersedes earlier agent-default, tenant-not-authorized and failed-only connectivity notes.

## Latest — bounded discovery after owner authentication

Owner explicitly authorized agent-only GET profile then active loads at https://carrierview.com.
Added a narrowly allowlisted envelope-discovery path without marking typed response mappings verified.
It reuses bounded transport, strict success parsing and redacted audit; it cannot import or write.
Only candidate references/fingerprints may be reported; raw successful evidence stays under runtime.
Profile failed with user_not_found; stopped before active loads, no fallback or retry.
Trim DPAPI encoded text before decoding because the storage helper adds a newline; suppress raw
decoding errors. No credential content is changed, printed or stored in source.

## 2026-09-07 — M1 retained

FreightDesk owns canonical facts, policy, authorization, approvals, scheduling and audit;
external platforms remain adapters. Python/FastAPI/Pydantic and vanilla dashboard keep the
local foundation small. SQLite tenant keys and transactions persist lifecycle/events.
One process only. Full lifecycle/gates precede incremental execution. Fixtures use DEMO-*.
ALLOW supports simulation without a click; unknown facts/actions fail closed.
Production startup is disabled; local bootstrap is demo-only. Models/workers are replaceable
contracts, not implemented external services. No commit/remote was created.

## 2026-09-07 — CarrierView implementation

1. Use the owner-supplied endpoint/auth/error contract. Do not guess HTTPS origin or response
   nesting: explicit ResponseContract selectors and verification flags precede networking.
2. Source remains in OneDrive; all new runtime state uses owner-authorized C:\FreightDeskRuntime.
   Reject other roots/path escapes and reparse points. Migrate synthetic demo state without loss.
3. Two DPAPI files and environment names. Agent is default; tenant requires explicit selection
   and elevation reason. Permission denial raises scope/elevation; never fallback.
4. HTTP success requires strict success=true, not just 200. Redirects and environment proxies
   are disabled. Bound time, bytes and local history count; sanitize errors/audit.
5. Implement documented writes through an immutable local action ledger, but block all production
   write networking. Recheck actor/tenant/policy/approval/pause/agent state/shipment version/contact
   and payload before claiming. Ambiguous/abandoned writes become UNCERTAIN, never auto-requeued.
6. Share the conservative SMS attempt budget across credential classes; no automatic retry,
   including 429. A ledger is a safety foundation, not proof of provider idempotency.
7. Preserve raw provider fields/nulls and source timestamps. Provider ID differs from Booking
   load_id. Retain tracking/client URLs as provider metadata. Do not infer acceptance from
   undocumented app codes or freshness from tracking-request time.
8. Require known-load and account identity checks before staging. UI reconciliation is a separate,
   recent, hash-bound step before import; fixture evidence cannot become LIVE POC #001.
9. Keep a protected local live projection distinct from demo data. A separate owner view token
   is required; never ask the dashboard for a CarrierView token.
10. Separate localhost webhook inbox from dashboard. Normalize, authenticate locally, associate,
    deduplicate, preserve timestamps and audit, but quarantine until vendor wire/auth is verified.
11. Documents/POD remain unsupported/unknown. No undocumented endpoints or separate initiate call.
12. Stop after tests for the owner to enter tokens privately. No live request before confirmation;
    eventual first test is GET-only and bounded to the known shipment.
# M3 decisions — 2026-09-07

Use a single-tenant Microsoft public client, system-browser PKCE and Windows DPAPI token cache.
Initial OAuth plan requested User.Read + Mail.ReadWrite; the owner update below adds MailboxSettings.Read.
Mail.Send remains excluded from OAuth/execution. Exact mailbox profile verification gates reads.
Implement per-folder immutable-ID delta with atomic receipts/events/proposals/checkpoints; no scheduler
or public webhook until explicitly commissioned. Unknown draft outcomes are durable and never auto-retried.
Persist extraction evidence as proposals; resolve ambiguous ETA/reference/sender identity before mutation.
Historical CSV/XLSX import has an explicit reviewed schema and separate runtime database; Decimal
statistics carry currency/sample/date/source provenance and cannot authorize a price commitment.
No vendor/live claims arise from fixtures. M3 ends at the user-requested Microsoft manual-auth boundary.

## Owner-confirmed Entra registration and mailbox-settings scope update (prior checkpoint)

The owner completed Avery Desktop registration and provisioned delegated User.Read, Mail.ReadWrite,
MailboxSettings.Read and Mail.Send; all display Admin consent required: No. Retain Mail.Send provisioned
for later but never use the registration permission list as OAuth scopes. Current interactive/silent
acquisition explicitly requests only User.Read, Mail.ReadWrite and MailboxSettings.Read, without .default
or extra consent scopes. Existing production send guards remain unchanged and independent of ActionPolicy.

Add a typed bounded GET /me/mailboxSettings selecting timezone/language/date/time format after exact
mailbox profile verification. Prepared login and bounded-validation helpers preserve its observation
privately. Treat Windows/IANA timezone strings and locale as preferences, never inferred freight facts;
do not change settings or invent offsets, dates or AM/PM. No live requests are authorized in this update.
Configuration/contract/docs distinguish Entra configuration, next OAuth request, ActionPolicy/execution
and live validation explicitly. No Microsoft capability is LIVE_VALIDATED. Full suite: 211 passed;
Ruff and both JavaScript rendering checks pass. Next action is owner-run private setup/login, not an
agent network probe. No registration identifiers, secrets or real token cache were read/exposed.

## M3 owner-confirmed bounded production evidence — current checkpoint

Evidence source: the owner's completed bounded-production-run report and manual Outlook
Drafts-folder confirmation. This documentation update made no additional Graph requests and did not
inspect tokens, message bodies or runtime payloads. No timestamp, message IDs, correlation results or
attachment evidence have been invented. No email was sent; no Ascend, CarrierView or canonical
shipment write occurred. Exactly one unsent draft is the only reported provider write.

Promote only OAuth authentication, exact mailbox verification, mailbox settings read, bounded
production message discovery/read, real-message ingestion/processing and unsent draft creation.
The run processed 5 messages, stored 0 attachments and created exactly 1 unsent draft; the owner
confirmed the draft in Outlook. Do not generalize this evidence to extraction accuracy, attachment
handling, incremental delta, real shipment matching, reply/forward drafts or autonomous execution.
No additional Graph networking was performed or authorized in this documentation update.

Mail.Send remains configured in Entra, excluded from the current OAuth scope set, not authorized
for execution and NOT LIVE_VALIDATED. Existing send HTTP guards remain unchanged. No production
sending or Ascend/CarrierView/canonical mutation occurred. Full suite remains 211 tests, with Ruff
and both JavaScript rendering checks passing. Implementation/test status is independent of the
six explicitly scoped live observations recorded in LIVE_POC.md.

## M4A actual Ascend CSV staging and approval boundary

Inspect the actual 694-row / 59-column owner export before proposing its mapping. Dedicated typed
AscendHistoricalEvidence preserves raw labels, all raw columns, source hash/load ID and source dates.
Keep total income, total expenses, gross profit and margin separate; carrier pay remains unknown.
Currency remains unconfirmed until owner review. Staged and independent source financial sums both
match owner anchors; all row gross-profit and two-decimal margin checks pass (ROUND_HALF_UP).

Stage only in the existing isolated historical database using separate ascend_stage_* records.
No real commit was performed. Repeated identical source yields one batch and 694 evidence rows;
synthetic tests cover changed-source conflicts and prevent silent overwrite. 110 raw lanes become
109 only under a proposed trim/casefold grouping. Preserve source MC prefixes/zeros/whitespace,
literal MC placeholders, unknown local timezones, missing delivery date and empty columns.
Exchange Rate Date cannot supply event, payment or FX-effective timestamps.

Propose aliases/equipment separately. Three Tube Supply labels (326 loads) remain distinct.
Scoped note review yields only unverified topics; no model call, source-note disclosure or SOP change.
Owner must approve the hash-bound mapping and financial semantics before historical commit and
post-commit profile/rate validation. All CarrierView/Microsoft evidence and send guards remain unchanged.

## 2026-09-08 — M4A owner approval fulfilled: historical commit and intelligence

Owner explicitly approved all 694 staged rows, their proposed schema and USD income/total expenses/
gross profit for history.sqlite3 only. Source SHA-256 is
af7e479a94f44f7f7541e2ecdd725de48e37656f2a0da92085670e16d0886bb0; batch is
090676423701034e909dc695d1920546c7c410b4697877c799141aebee0a81d4.
The approval receipt binds source, mapping digest, row count, currency and eight retention boundaries.
Re-read source and compare staged raw/typed evidence before one transaction. Preserve the original
proposed mapping and null source currency; owner-confirmed USD is separate committed provenance.
Never interpret total expenses as carrier pay, merge aliases/equipment, infer zones/dates, normalize away
identifier evidence or use Exchange Rate Date as an event. Historical records never update live shipments.

Actual commit inserted 694 records. Re-import inserted zero and left the record/audit hashes identical;
SQLite integrity passed. All 694 arithmetic and rounded-margin checks passed. Income USD 1,693,484.10,
total expenses USD 1,378,557.00 and gross USD 314,927.10 match independent CSV sums. Pickup coverage
2024-09-03–2026-09-04; all figures here describe this raw-label sample and derived aggregates, with
timestamp and exact row evidence in runtime historical-intelligence.json / commit-verification.json.

Generated real customer/lane/carrier/facility and rate-history profiles. Missing carrier/equipment
identity samples are separate, never fictitious entities. Endpoint facilities are exact raw tuples,
not verified legal entities or intermediate stops. Statistical provenance includes sample size, date
coverage, calculation time, source/batch/row/hash and raw-versus-derived designation. Internal history
does not establish market rates or quotes. Candidate note topics are unverified keyword evidence only.
Changed-export version behavior remains synthetic-tested, not LIVE_VALIDATED. No vendor networking,
Outlook/CarrierView/Ascend writes, canonical updates or automated quotes occurred. Prior M2/M3 guards stand.

## 2026-09-08 — M4B offline foundation and first live boundary

The owner authorized implementation/offline tests and explicitly required stopping before real Ascend
login/navigation/write. Implement a replaceable AscendAdapter using deterministic browser primitives;
no general CRUD API or live DOM selector has been independently established. Do not invent either.
No real Ascend navigation, read, API call or write occurred. Playwright docs/package access and local
synthetic browser fixtures are development evidence only, never Ascend LIVE_VALIDATED evidence.

Use a dedicated runtime Avery profile, one-use exact-load grant and owner-verified company/account/
origin/DOM contract. Start offline, close restored tabs, install strict same-origin read routing before
online navigation. Owner login/MFA is manual and separately authorized. No owner profile/cookie reuse,
screenshots, DOM dumps, tracing, private note/phone logging or automatic recovery. Profile prepared
but never launched; proposed first load 1752 still needs owner selection. No broad loads listing.

Keep raw provider facts with explicit normalization/provenance and derived reconciliation separate.
Historical/canonical/CV/mail evidence is never overwritten. Missing or unmapped facts stay unknown;
Outlook is unavailable without explicit real shipment correlation. Historical evidence is stale for
current-state proof; other evidence older than 24 hours is marked stale. Raw appointment comparison
does not infer a timezone or semantic equivalence. Current currency/prices are not inherited from M4A.

All real mutation interfaces remain hard-blocked. The fixture coordinator checks account/exact load,
reread/version, policy, pause/takeover, payload and bound approval; it claims durably, executes once,
rereads and audits. Crashes/ambiguous results remain UNCERTAIN without replay. Trusted ALLOW policy
can execute a fixture without a click; pricing still requires owner approval. Future create absence/
idempotency, mutation DOM screens and upload validation require separate verified implementation.

Extend the existing protected local dashboard, with no browser actions behind HTTP. Document disabled
Outlook tender and BOOK IT workflows using separate future adapters; no customer quote automation.
290 tests, Ruff and all JS checks pass, including real headless DOM/session and dashboard fixtures.
All six M4B phase-one capabilities and every production write remain NOT LIVE_VALIDATED. M2/M3/M4A
matrices and Microsoft send guards are unchanged. Exact next commands/criteria: docs/ASCEND_ACCESS.md.

## 2026-09-09 — owner authorizes exact-load M4B phase one

Owner selected Booking 1752 and explicitly authorized dedicated-profile launch, manual login/MFA,
account verification, exact-load read/extraction and read-only local reconciliation/evidence. No
operational Save/Submit, assignments, status/rate/note/document/communication or canonical mutation.
Initial helper failed before the manual-login prompt. A startup-tab race was corrected: open the
replacement blank tab while offline before closing startup tabs. Isolated checks and 291 tests,
Ruff and JavaScript checks pass. The dedicated window is now open awaiting the owner; no account
identity/load read is yet proven and no M4B capability is marked LIVE_VALIDATED.


## 2026-09-09 — phase-one identity gate enforced

Owner supplied confirmed origin https://ascendtms.com/ and a manual-login dashboard screenshot.
The automated reader reopened only the dedicated profile with GET/HEAD same-origin routing, but
could not establish expected account identity from its top-level DOM. Do not treat screenshot-only
evidence as proof of the controlled reader's session, or infer the reason for the DOM/session gap.
Stopped before exact-load navigation, froze browser networking and stored sanitized local audit at
ascend/phase1-1752-verification-stop.json. No load data, operational writes or canonical mutation.
No M4B capability is promoted to LIVE_VALIDATED. Existing 291-test/JS/lint result remains the last
software verification; this follow-up changed documentation and private audit only.


## POC #002 — owner workload takes priority, offline preparation and owner-read boundary

Owner reports 1760 delivered, ~8 pickups/~3 deliveries tomorrow. Prioritize actual operations over
historical 1752 proof; counts are expectations, never fabricated rows or exact-count constraints.
Implement a one-use dated same-process board/detail reader with explicit calendar/date-format input,
fixed read actions, normal app networking, exact discovered identities and partial coverage reporting.
Keep provider facts/private contacts, owner reports and derived queue states separate in operations
runtime storage. No canonical import/mutation is needed to prepare an operational proposal.

Prepare bounded tenant GET reconciliation after owner Ascend execution, never agent fallback. Unverified
pagination means a list miss is not absence; do not create duplicates. Unknown status meanings remain
unknown. Prepare payload hashes/proposal-only durable ledger for qualifying facts; block dispatch and
surface unresolved company wire mapping/creation communication effects before a pilot approval request.
No pilot selected before real evidence, no write/SMS/email/chat/API mutation or batch release performed.
Created POC002 planning record with null pending queues. Owner command is in the dedicated handoff.
352 offline tests plus lint/JS checks pass, two prior warnings; focused date/readiness regressions pass.

## 2026-09-10 — owner-directed application networking and bounded DOM field discovery

Owner identified restrictive networking as the leading failure hypothesis and explicitly replaced it
as the same-process safety boundary. Allow normal browser app traffic and service workers, without
HTTP verb/origin/websocket filters or cookie/traffic-body instrumentation. Preserve fixed read actions,
one-use grants, session/exact-load/pause/policy/expiry gates and independent production mutation blocks.

Implement observed field discovery instead of requiring a guessed/preexisting selector file. Search/open
only 1752 with semantic read controls; verify detail identity before other values; inspect bounded approved
tabs/labels and repeat snapshots. Record AscendFieldContract provenance/version and metadata-only proposals.
Accept mappings only for observed unique labels and matching, nonconflicting independent values, scoped
to 1752. Unsupported/ambiguous/different candidates stay UNKNOWN. No general provider-schema promotion,
private field persistence or canonical mutation. No model/page content can extend the fixed read surface.
Only offline tests were run. Owner must execute owner-same-process-1752-01; bootstrap -02 remains reserved.
No new LIVE_VALIDATED evidence or cross-process reuse claim.
Verification: 342 offline tests plus Ruff/JavaScript/dashboard checks pass; two existing warnings.
Bounded DOM hydration waits do not reload or repeat a click and never ignore a conflicting load identity.

## 2026-09-10 — offline continuity and same-process read orchestration

Owner requested continuity diagnosis without another live action or consuming bootstrap -02. Compare
shared launch settings: Python persistent Chromium/msedge, same Default profile, headed, identical
arguments, JavaScript enabled, workers blocked, no proxy override, default extensions disabled. No
configuration drift found; retain all existing read restrictions. Material post-launch differences:
manual login has normal networking; controlled reads block off-origin/non-GET/HEAD traffic, and startup
replaces restored tabs. Resource blocking and tab/process-scoped auth loss remain competing hypotheses.
There is no prior before/after aggregate baseline; do not inspect secrets or run a live browser to invent one.

Implement diagnose-continuity to capture aggregate auth storage immediately after future owner-confirmed
login, persist that baseline, then reopen once and compare identical counts plus sanitized passive
resource/error metadata. No names/keys/values/HTML/payloads or raw errors; no load grant or load navigation.
Implement login-validate-1752 to keep the exact owner-confirmed page/context and invoke the existing read
adapter without relaunch, pre-read closure or account-root reload. It verifies the same session gate and
retains one-use grant, pause/policy/expiry/exact-load/verified-contract/reread and independent write guards.
Reserved -02 is explicitly rejected. No cross-process persistence requirement is added to a first read.
Existing field contract still needs verified live DOM selectors; the implementation must stop if absent.
No production actions occurred this turn and no LIVE_VALIDATED state changed. All new evidence is offline.
Verification: 332 tests, Ruff and all JavaScript/dashboard checks pass; two existing dependency warnings.

## 2026-09-10 — structural diagnostic without a load attempt

Owner requested a separate bounded render/all-frame diagnostic and explicitly prohibited validate-1752.
Added diagnose-session with a fixed root origin and existing Python Playwright/msedge profile. It neither
accepts a load attempt ID nor consumes a read grant. It polls after an initial render delay, up to 15 seconds;
two consecutive positive samples are needed for a positive recommendation. All frames supply sanitized
structure/login checks; positive navigation must be in a same-origin frame. Icon-only route navigation
is assessed separately from visible labels. Root links alone cannot prove authentication. Unknown frame
paths/titles are redacted; no query strings, raw DOM/text, customer values, secrets or traffic are saved.

Live structural diagnostic: /, AscendTMS, complete, 8 body children, 23 text characters, one frame, no
login form/nav labels/authenticated-route links after 15.011 seconds. Session recommendation remains
false; provider tenant identity unverified, Booking Logistics OWNER_ATTESTED. No load opened or attempt
-02 consumed; no production writes. No automatic retry or load gate change. Missing app DOM cause remains
unknown. Runtime structural-diagnostic-20260910T120152548406Z.json records the sanitized evidence.
All 322 tests plus Ruff and JavaScript checks pass; two existing dependency warnings. Six added tests
cover delayed rendering, all-frame/login behavior, text-free route evidence, empty DOM and no load grant.

## 2026-09-09 — same-profile/channel verification and identity-only mode

Subsequent decision 2026-09-10: the owner-authorized Customer Zero bootstrap now separates explicit
OWNER_ATTESTED Booking Logistics identity from deterministic provider/browser session authentication.
The verified origin remains https://ascendtms.com; no new origin/login request. A login-free page must
also expose Dashboard, Loads and two additional approved nav controls in one same-origin frame. Company
text is not mandatory in this branch. No fabricated provider account hash, tenant verification or
session-reuse claim is derived from owner attestation. All exact-load, pause/policy and write guards remain.

Added one-use validate-1752 workflow and five-field sanitized diagnostics. Field reconciliation uses
only existing local historical/CV evidence and requires an observed verified field contract; no fixture
selector promotion. Bootstrap stores only provenance/comparison metadata, not raw shipment values.
Authorized attempt owner-bootstrap-1752-20260910-01 stopped at authentication: /, AscendTMS, no login form,
no approved nav markers. No /loads navigation or read, no write or canonical mutation, no automatic retry.
Owner attestation is recorded; all M4B provider capabilities remain NOT LIVE_VALIDATED. The underlying
absence of app navigation is unresolved and is not proof of wrong tenant or expired authentication.
Verification: 316 tests, Ruff and JavaScript checks pass; two pre-existing deprecation warnings.

Historical 2026-09-09 decision follows:

Owner required local persistent Python Playwright only, no Computer Use or alternate browser.
Review confirmed manual login and bounded reader already use AveryBrowserSession/msedge and exact
approved user_data_dir. Earlier Node troubleshooting had deviated from the shared driver; close it
without navigation and use Python only. Add existing-profile enforcement to validation and an
identity-only command accepting a strict bare HTTPS origin. No new/incognito profile or cookie import.
Presence checks find preferences, cookie database and local storage but never read cookie values.

The authorized identity test against owner-provided https://ascendtms.com stopped with no expected
company/user/dashboard indicators. It read no shipment and performed no operational/canonical write.
Do not infer the cause or promote M4B LIVE_VALIDATED. Save sanitized identity-only-result.json.
Full suite: 303 tests plus Ruff/JavaScript checks pass; two existing dependency warnings remain.
# 2026-09-10 — separate All Loads schema diagnostic

Treat authenticated board access, owner-attested tenant identity, table schema and operational reads
as separate evidence. Confirmed owner-live-ops-001 consumed and header-parser stop. Prepare a new
owner-table-schema-001 read-only diagnostic, never retry the manifest parser unchanged. Same existing
local Python/msedge page/context and normal networking; fixed navigate/DOM-only surface, no details.
Store versioned schema proposals with snapshot selectors, explicit uncertainty and sanitized date/load
observations under runtime only. No live diagnostic execution or capability promotion this turn.
# 2026-09-10 — use observed 33-column board contract

Use owner-table-schema-001 as observed AscendAllLoadsTableContract v1 evidence. Do not bind table index 3.
Validate all supported named headers and widths; reject missing/drifting/ambiguous data grids. Extract
only requested fields from service-date-matched rows and store them privately with column/run provenance.
Queue membership uses Pick Date/Drop Date independently; no detail-schema prerequisite for board queues.
Stop/phone/communication readiness remains blocked by missing evidence. Explicit pagination DOM proof
may support COMPLETE_CURRENT_BOARD only. No live run or attempt consumption in this offline correction.
# 2026-09-11 — minimal Active Loads failure-reporting correction

Preserve the existing failed artifact unchanged; do not invent a retrospective error code or stage.
Correct Python href prefix method startsWith to startswith in view selection. Record bounded workflow
stages and fixed exception-class error codes/reasons, never provider text, exception messages or private
payloads. The observed consumed attempt has no persisted candidates/manifest and no exact cause evidence.
No weakening of Active Loads authority, reconciliation or write guards; no new live attempt created.
# 2026-09-11 — prepare exact-eleven tenant GET reconciliation

Bind the CarrierView read to owner-active-loads-20260911-02 and its exact approved pickup/delivery sets.
Preserve VISIBLE_BOARD_ONLY scope; owner reconciliation is sufficient for this bounded read, not writes.
Use only existing active/past discovery GETs and exact matched IDs (14 max), with tenant audit and no
fallback/retry. Save a separate sanitized reconciliation instead of overwriting the original manifest or
invoking proposal creation. Unknown existence/status/integration/document semantics stay unknown.
No live CarrierView run occurred. No pilot without verified absence and complete phone/stop evidence.
# 2026-09-11 — staged multi-system read control plane

Prepare separate one-use owner-executed Ascend, CarrierView and gap-only Outlook read phases, then a
local evidence-led report. Reuse Active Loads exact scope, not stale All Loads facts. CarrierView
active/future scope is supplied by owner; no past query in this run. Keep vendor facts, email claims,
derived assignment/classifications and owner oracle separate. Only compare 7/1/3 after decisions.
Missing/ambiguous evidence queues review. Bounded misses may support pilot-review eligibility under the
new owner definition, but do not prove absence or enable execution. No write/autonomous-send capability
is introduced. All live phases remain unexecuted in this implementation turn.
# 2026-09-11 — retain uncertainty for uninstrumented Ascend phase failure

Do not manufacture an exact cause for owner-multi-system-20260911-01. The only persisted execution
evidence is the consumed Ascend grant. Add fixed-code, stage-specific, sanitized append-only progress
and failure records for future owner execution; no raw exception strings/tracebacks/provider data.
Per-load completed counts distinguish in-memory reads from end-of-phase committed evidence. Preserve
primary errors over cleanup errors. No changes to extraction decisions or partial evidence promotion,
no new run ID, retry or vendor networking. 25 targeted tests and Ruff passed.
# Separate Ascend-only diagnostic namespace

Prepare owner-multi-system-ascend-diagnostic-20260911-01, with a separate metadata-only diagnostics DB
and one-use grant. Never resume or mutate the original multi-system namespace. Persist each stage and
per-load completion immediately; stop on first failure. Verify exact start/end board counts and hash
before diagnostic success. Use existing bounded read executor with an optional sanitized observation
callback; no operational field payload persistence or new vendor action surface. Targeted tests only;
no live browser or networking during implementation.
# Active Loads canonical presentation state before detail reads

Use a separate diagnostic 02. Proven failure is board scope mismatch; no detail-contract change is
justified. Permit only bounded reversible DataTables search/page-size/first-page actions in the structurally
verified grid wrapper. Observe unknown filters and fail closed rather than guess resets. Reconcile exact
IDs independently of count and use a sorted semantic board hash that excludes presentation state.
No production write or other-system phase authorization. Preserve old attempts; owner executes next run.
# Identity-only 1755 observation before any operational extraction

Keep proven Active Loads normalization unchanged. Reuse fixed exact-row opener with an optional
sanitized pre-click metadata callback. A separate diagnostic observes only identity-related fields,
headings, attributes and row-linked routes. Select one unique primary source with consistent supporting
IDs; conflicts fail closed. Save a scoped live contract only after actual evidence, never adopt fixture
selectors automatically. No other ten loads, operational fields or external phases are invoked here.
