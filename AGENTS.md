LATEST 2026-09-13: owner requested fixes for audit A01-A10. Read docs/WEBBRIDGE_V2_AUDIT_FIXES.md.
Offline repair: coherent capture/field-node bindings, admitted private-safe text, document ledger,
compatibility3/graph2/wire1, strict graph coherence, field/domain checks, indexed bounded history,
adversarial fixtures and actual subprocess recovery. Baseline6d4a781 is AUDITED PRE-REPAIR, not a safe
live rollback claim. Source-hash/rollback helper stays TestRuns. Current test results: CURRENT_STATE.md.
V1 default; V2 absent from manifest, fixture/TestRuns gated and OBSERVE-only. No live package, installed
extension/host/profile, vendor, real lease, registry or permission action. Do not reuse closed grants.
No operational semantics/general mapping/AUTO_MAP or V2 LIVE_VALIDATED claim. Earlier acceptance below
was reopened by independent audit and is historical.

LATEST COMPLETION 2026-09-12: V2 Phases 0–2 accepted OFFLINE after owner proceed.
Read docs/WEBBRIDGE_V2_PHASES_0_2_ACCEPTANCE.md. 428 broader + 7 distinct boundary + 1 integrated
graph-bound tests passed (436 distinct); focused/final diagnostic runs overlap. Actual synthetic
X1 coordinator/host/worker/content/runtime-store chain passes.
V1 remains default; V2 is absent from manifest, TestRuns-gated and OBSERVE-only. No installed
extension/host/profile/vendor/lease/registry/permission action and no V2 LIVE_VALIDATED claim.
Pinned Playwright subset now attributed with LICENSE/NOTICE; older research-only statements below
are historical. Operational meanings and Phases 3–7 remain deferred. Do not reuse closed live grants
or automatically package V2 for live use.

EARLIER IMPLEMENTATION 2026-09-12: owner approved starting WebBridge V2 Phases 0–2 offline.
Draft core/contracts/TestRuns store and 61 focused tests exist; this is not integrated completion.
Read docs/WEBBRIDGE_V2_PHASES_0_2_REVIEW.md. Owner asked to compare the revised brief, not execute
its embedded live/future instructions. Preserve V1 defaults; no installed extension/host/profile,
vendor/lease/registry/permission changes. Real V2 pipeline integration/evidence schema remain pending.
No V2 LIVE_VALIDATED claim. Do not reuse the closed five-iteration live authorization.

EARLIER RESEARCH 2026-09-12: WebBridge V2 source audit/design is complete; implementation is not
authorized. Read docs/BROWSER_INTELLIGENCE_CODE_HARVEST.md and docs/WEBBRIDGE_V2_ARCHITECTURE.md.
Eleven repositories studied; no third-party code/dependencies incorporated. Proposed provenance
is RESEARCH_ONLY, not redistribution clearance. X1 remains sole authenticated sensor/executor;
no provider selector, transport, live-state or permission changes. Do not resume closed live work.
No V2 LIVE_VALIDATED claim. Further implementation needs owner approval.

LATEST COMPLETION 2026-09-12: the direct five-iteration X1 debug authorization is CLOSED.
All five iterations were counted; the final iteration succeeded for exact LOAD 1763 / Load Basics.
Signed worker/content 0.6.2, trace revision 1 and mapper revision 2 were observed. Phase A matching
dispatch/ACK, Phase B provider identity and Phase C one CANDIDATE_ONLY map with 19 metadata fields
passed. Values=false, writes=false. Authority REVOKED, cleanup COMPLETE; no post-revoke read receipt.
Do not start a sixth iteration, resume/review navigation or enable another lease without new
authorization. Read docs/ASCEND_X1_SESSION_CAUSAL_DEBUG.md. Historical DOCUMENT_CHANGED exact
branch remains UNKNOWN; never call it failed login. General Mapping Mode, AUTO_MAP and operational
field semantics remain unvalidated. No enrollment/permission changes. Final relevant suite: 411
passed; scoped Ruff/Node passed. Authorization and owner-manual/offline notes below are historical.
All production-write restrictions and security safeguards remain in force.

EARLIER OWNER AUTHORIZATION 2026-09-12: agent may perform up to five materially distinct bounded
live READ-ONLY debugging iterations through existing X1 for exact Load1763/LoadBasics. Read
docs/ASCEND_X1_SESSION_CAUSAL_DEBUG.md. Do not delegate live execution to subagents or ask owner
to run each command. Local Windows browser-chrome reload/focus is allowed; provider reads stayinX1.
No Computer Use/new profile/Playwright identity experiment/undocumented endpoints/operational writes.
Trace receipt proves fresh new-lease session then DOCUMENT_CHANGED; do not call it failed login or
guess LoadBasics selectors. Require PhaseA dispatch/ACK, then PhaseB provideridentity beforesectionwork.
Ordinaryjobs tracingoff; --causal-trace opt-in256events. Stop repeatedfailurewithoutnewevidence.
No new LIVE_VALIDATED claim until actual receipts supportit. Older owner-manual/no-live instructions
below are superseded only within this exact bounded authorization.

Earlier: X1 0.6.2 Mapping Reliability Fix Pack implemented offline. Read
docs/ASCEND_X1_MAPPING_RELIABILITY.md. No live run/lease/re-pair/registry/provider actions here.
Review findings1-7 corrected: precise section diagnostics, unchanged-document observer continuity,
fresh foreground readiness before capture deadlines, reversible read-job dependency wait,
candidate CROSS_LOAD_VARIATION, authenticated existing-pipe wake hints, normal safe failure/status.
Integrated actual coordinator/host/worker/router/content/reader/store path uses only synthetic
provider DOM/credentials and intercepted networking. Numeric timestamp MAC canonicalization fixed;
raw protocol scalar validation and authentication/replay remain strict. Real section relationships
remain UNKNOWN, no selector guesses/LIVE_VALIDATED promotion. Owner reload0.6.2, one command for
1763/Load Basics, then return focus; no manualID/Capture/re-pair. No writes or unverified traversal.

Earlier: X1 0.6.1 immediate CURRENT_LOAD handoff implemented offline. Do not run live Ascend.
Local audit proved queued/dispatched capture rejected MAPPING_OWNER_NOT_PRESENT; passive OBSERVE
handling hid the error until expiry. Do not call it a DOM/load/session/transport failure. CURRENT_LOAD
now rebinds and dispatches in the same wake, uses distinct queue/dispatch/ACK/completion evidence,
and short stage deadlines. Preserve owner presence and target gates. Owner reload/one-command retry
only; no re-pair/new manual attempt ID. See docs/ASCEND_MAPPING_ORCHESTRATOR.md.

LATEST: Mapping Orchestrator V1 / X1 0.6.0 implemented offline. Read
 docs/ASCEND_MAPPING_ORCHESTRATOR.md. The owner entry is Map Ascend or
 scripts.ascend_mapping_orchestrator start; IDs, preflight, leases, capture/report/cleanup are internal.
 No live job/start/review/qualification/vendor run here. Prior manual -02 remains unused.
 Keep exact identity, metadata-only scope, write blocks and general AUTO_MAP cohort qualification.
 Current operations is gated on unverified Planning/accounting provider view contracts; no crawler.
 Do not restart the old manual harness as ordinary owner workflow. Earlier instructions are historical.

LATEST: X1 0.5.1 OBSERVE diagnostics are offline only. See docs/ASCEND_X1_OBSERVE_RETRY.md.
Do not enable AUTO_MAP, launch Ascend, consume owner-x1-mapping-20260911-02, or re-pair here.
Prior mapping session -01 timed out with zero maps; internal stage is UNKNOWN. New retry is
owner-executed, OBSERVE first-validation cohort 1763,1755,1769,1737 but capture restricted to
1763 / Load Basics, one attempt. Preserve audit; no automatic retry or LIVE_VALIDATED promotion.

# Latest: X1 0.5.0 OBSERVE + AUTO_MAP, offline only

Read docs/ASCEND_X1_MAPPING_MODE.md. OBSERVE passively learns unknown owner-visited sections;
AUTO_MAP traverses reviewed VERIFIED READ_ONLY_NAVIGATION controls within one exact current LOAD,
with fresh session/lease/workspace/control checks per transition and a verified return to start.
No model selectors/scripts, writes, other-load navigation or crawler. The 3-5 ID cohort is validation
only, not a permanent limit. Normal modes accept no ID list by default; normal AUTO_MAP requires
successful owner-reviewed cohort cycles. OBSERVE alone cannot qualify it. Metadata-only field maps
remain CANDIDATE_ONLY/UNKNOWN pending separate read validation. Keep Active Loads/VISIBLE_BOARD_ONLY.
No live execution/lease enable/navigation approval/qualification/re-pair/vendor calls here. Owner
future reload0.5.0 and controlled OBSERVE-then-AUTO_MAP procedure only. Old panel-detail -03 superseded.

# Latest: X1 0.4.3 causal detail containers, offline only

Read docs/ASCEND_X1_CAUSAL_CONTAINERS.md. Owner detail -02 consumed; exact1755 FIND succeeded,
then OPEN_READONLY failed DETAIL_BOUND_CONTAINERS (old max12, measured/category not saved).
Do not revisit healthy enrollment/host/scheduler/session/view/board schema. Causal roots now rank
direct/new/newly-visible/connected-selection; bounded4/8/8/4, identity64 unchanged. Equal top ranks
stop; provider identity required before metadata mapping. -03 is proposed unused only; never queue,
enable a lease or execute live here. No re-pair or board-contract change. Detail UNKNOWN/not live
validated; VISIBLE_BOARD_ONLY and all writes disabled. Future owner reload adopts0.4.3.

# Earlier: X1 0.4.2 board-schema correction, offline only

Read docs/ASCEND_X1_BOARD_SCHEMA.md. 0.4.1 lifecycle/session/view succeeded; subsequent board
failure recorded only BOARD_SCHEMA_INVALID and view proof, no predicate/counts. Do not invent the
live mismatch or reuse retained board evidence as current. Source0.4.2 rejects header-clone
competition, maps full headers semantically, retains all schema/write gates and persists safe
structural diagnostics/predicates. Latest inspected lease was owner-revoked 22:31:43 UTC. Prepare
reload plus short fresh lease only; never execute it here, re-pair, refresh Ascend or queue details.
No 0.4.2 live validation. Historical board scope/VISIBLE_BOARD_ONLY/UNKNOWN detail mappings remain.

# Earlier: source X1 0.4.1 lifecycle repair, offline only

Read docs/ASCEND_X1_CONTENT_LIFECYCLE.md. Persisted REFRESH_ASCEND_TAB incorrectly blocked later
handshakes. Fixed packaged reinjection replaces refresh retries; current vs retained board evidence
is explicit. Do not ask for tab refresh, re-pair or re-enrollment. Source adds scripting API capability
with unchanged exact Ascend host scope. Owner must reload to adopt it; no live execution here.
Keep detail attempt untouched and detail mappings UNKNOWN. No write gate changes.

# Latest X1 detail contract preparation - offline only

Read docs/ASCEND_X1_DETAIL_CONTRACT.md. Source0.4.0 adds metadata-only, candidate-only one-load
DOM discovery behind exact provider identity and fresh board/lease gates. Do not execute owner
steps, enable read lease or consume owner-x1-detail-20260911-01. No field contract is LIVE_VALIDATED.
The board runtime promotion (three cycles/one proposal/two no-ops/revoke) remains valid. Source
changes need a future owner reload, not re-pairing. No raw private fields/notes/finance in diagnostics.

# Current override - X1 0.3.1 (2026-09-11)

OFFLINE ONLY unless separately owner-executed. Read docs/ASCEND_X1_TAB_LIFECYCLE.md.
Owner-tested enrollment/reconnect and persisted session receipts supersede older no-live-enrollment
claims below. One eligible root-path tab was automatically bound; navigation then READ_TIMEOUT,
not a proven tab-discovery failure. No board capability validated. Lease expired 19:09:30 UTC.
Do not re-pair, reset enrollment, launch vendor/browser or enable a lease during coding. Extension
alarms own wake; host owns read policy. Auto-refresh stale content only at exact root/loads without
query/fragment, once, with current host lease. Independent view/identity/write gates remain intact.

# FreightDesk coding instructions

LATEST 2026-09-11 OWNER OVERRIDE: X1 0.3.0 persistent read-only runtime is IMPLEMENTED OFFLINE.
Read docs/ASCEND_X1_RUNTIME.md. One explicit owner enrollment -> durable DPAPI record bound pinned
opckmnldaebecjphbmdmelfflikinpif/Windows user+device/host installation/tenant actor/protocol. Edge stores
opaque enrollment handle only; HMAC keys memory-only. Auto native reconnect and safe tab recovery
are runtime behaviors AFTER enrollment; owner must explicitly enable first real read lease (default8h).
No real enrollment/bootstrap/lease/reload/host/browser/vendor/registry action this coding task.
Preserve old consumed pairings and -01/-02 grants; no reset/migration deletion. New lease/scheduler/
receipts/proposals use separate ascend-native/runtime.sqlite3, never canonical mutation. The host
owns schedule, policy/pause/expiry/replay/security gates, provider view/session/identity proof remains
mandatory. Typed NAVIGATE_ACTIVE_LOADS onlyknownproviderUI; no arbitrary selectors/URLs/JS or writes.
Operational mappings stillUNKNOWN fields[]; implementeddetailreadroutes do not prove actual values.
HashcoversID/pick/dropdatesonly; visibleboard<=100, no8/3oracle, noassignmentchange/wholeaccountinference.
No newpersistent-runtime LIVE_VALIDATED promotion. Playwrightidentityexperiments remainCLOSED. Older
one-use operating-model restrictions below are superseded by this owner-authorized offline design.
Final combined verification: 232 targeted tests passed; Ruff, Node and synthetic dashboard QA passed.

LATEST 2026-09-11: X1 -01 live read consumed; session receipt succeeded, then GET_ACTIVE_LOADS stopped
ACTIVE_VIEW_UNVERIFIED before board extraction. Read docs/ASCEND_X1_VIEW_CONTRACT.md. Actual live view
and missing marker remain unknown; 0.2.0 saved no candidates. Source 0.2.1 adds independent provider
DOM view contract/host verification/safe diagnostics/view-change stops; parent li.active and
aria-current=true restored. Never infer Active Loads from /loads, IDs or 8/3 counts. 117 targeted tests
pass, no new board/detail capability LIVE_VALIDATED. STOP: no live retry, re-pair, new bootstrap/grant,
registry/transport change or browser execution here. Old selected-tab port invalidated; last verified
pairing expired. Future Reload needs renewal and reselection, only with later owner authorization.
Unused owner-x1-identity-1755-20260911-02 is proposed, not created; explicit owner-only --supersedes-attempt
may append that one successor to reviewed consumed -01, never reset or authorize a third attempt.
Preserve all read/write/detail/tenant gates and Playwright closure. This overrides older pending-first-
test and immediate reload/pair instructions below. No operational extraction, CarrierView or Outlook.

LATEST 2026-09-11: owner reports X1 0.1.2 PAIRED / PAIRING_SUCCESS. ONLY local Edge extension <->
Native Messaging <-> FreightDesk host transport is LIVE_VALIDATED, provenance OWNER_REPORTED.
Source X1 0.2.0 adds the offline-tested four-command identity-only controller. Read
docs/ASCEND_X1_READONLY.md for the current handoff; older pairing/startup/read-release statements below
are historical. No actual Ascend extension read is validated. No browser/vendor execution, registry
change, real bootstrap or read grant may run automatically. Owner reloads existing extension/refreshes
the intended Active Loads tab, pairs afresh and executes the prepared local authorization CLI, then
explicitly selects the tab and presses each command once. Host-owned one-use 10-minute grant; exact
2026-09-11 US date / 8-pickup, 3-delivery oracle for this case only. First dispatch consumes the test;
no reset/retry after uncertainty. Final state IDENTITY_VERIFIED_READS_BLOCKED; operational details,
CarrierView/Outlook and writes remain blocked even after provider-backed 1755 identity. No page/model
content can expand commands, URLs or selectors. Preserve DPAPI/HMAC/replay/pause/policy gates and
OWNER_ATTESTED tenant identity. Playwright identity experiments remain CLOSED. Offline fixture results
do not promote vendor capabilities. Actual runtime ledger remains untouched until owner execution.

LATEST: local registry/manifest/audit inspection explicitly authorized and completed. Both Edge HKCU
views are correctly pinned; audit reached ONE_TIME_VALIDATION, no proof/consumed pairing. CopyTo relay
buffering reproduced offline and fixed in source. Read docs/ASCEND_NATIVE_STARTUP.md. Installed host
binary remains unchanged. RebuildLauncher and ascend_native_selftest --owner-executed are OWNER-RUN
only; do not run installed host, modify registry, generate bootstrap or retry pairing here. No extension
reload/re-registration needed for this host-only patch. Startup logs contain fixed stages/codes/types
and booleans only, no frames/secrets. Native synthetic ping is process-flag-only, no DPAPI/grant/vendor
access. Keep Playwright identity closure and all read/write guards. Actual Edge path awaits owner test.

LATEST: owner reports X1 0.1.1 registered/reachable, import stuck at PAIRING_REQUIRED. 0.1.2 pairing-only
instrumentation is offline-tested; docs/ASCEND_NATIVE_MESSAGING.md. No actual ID/bootstrap/DPAPI/ledger
inspection, Pair/registry/browser/vendor execution in this task. Persistent packaged panel + explicit
Import, safe codes, verified ACK and six-field pairing_attempts audit; no READ_ONLY_READY or read/write
release. Original live pairing failure is unproven; don't claim diagnostics prove its cause. Owner
reload/fresh Pair only after this handoff, not automatically. Existing Playwright identity closure stands.

LATEST 2026-09-11: owner reports Edge X1 0.1.0 installed successfully. 0.1.1 native foundation is
OFFLINE only: read docs/ASCEND_NATIVE_MESSAGING.md. Exact installed ID is captured only by owner-local
input, no guesses. Prepared scripts/ascend-native.ps1 requires OwnerExecuted; do NOT run CaptureId,
Register, Pair, Reset, Uninstall or real connectNative during this task. No actual registry/pairing
or browser/vendor action. Targeted tests only, fixture state under Runtime Data/TestRuns. DPAPI key,
one-use bootstrap/installation/audit only under C:\FreightDeskRuntime; never browser persistent storage.
PAIRED is local channel only. Host read_release_required and all production switches stay closed;
READ_ONLY_READY is reserved, not reachable. Future live plan needs policy/controller/tab lifecycle and
identity-only receipt release, fresh owner 8/3 board then exact1755 identity then STOP, no details/writes.
Playwright identity experiments remain CLOSED; no new grant or LIVE_VALIDATED promotion.

LATEST: final Playwright detail-identity attempt FAILED and is CLOSED (board_tables 9 > 8).
Never raise bound/retry/reset/create another Playwright identity attempt. Extension X1 prototype only;
see docs/ASCEND_EXECUTOR_PIVOT.md. No live Ascend networking or production connection. Host restricted
MV3 skeleton, Native Messaging foundation now implemented offline; actual host installation absent. No operational writes,
no generic JS/selector commands, no row-bound detail identity weakening or LIVE_VALIDATED promotion.
Keep existing Playwright board code. Future extension validation requires separate owner authorization.

LATEST: owner authorizes PREPARATION ONLY of owner-ascend-final-identity-1755-20260911-01.
Do not launch it here. Exact CLI ID + owner-authorized required; one-use final_causal_grant, prior
consumed grants untouched. Any unsuccessful final attempt ends Playwright identity experiments:
no new attempt/reset. Prepare extension-bridge vs provider-evidence-supported row-bound assessment
instead. No operational extraction, CarrierView/Outlook calls or writes. See ASCEND_TARGETED_IDENTITY.md.

LATEST: STOP live Ascend; causal attempt -01 consumed at BOARD_RECONCILED / causal_scan_bound with no
bound measurement/opener checkpoint. See docs/ASCEND_TARGETED_IDENTITY.md. Targeted refactor only,
offline tests; no new attempt ID or command. CLI live entry disabled before runtime/browser access.
One future owner-authorized final targeted attempt at most; failure mandates pivot away from Playwright
identity experiments. Preserve exact identity gates, existing normalization and all write prohibitions.

LATEST: identity -01 owner-run consumed, detail_identity_missing, zero candidates/no contract. Prepare
owner-ascend-causal-identity-1755-20260911-01 only; see docs/ASCEND_CAUSAL_IDENTITY.md. Offline targeted
tests only. One exact row/open; sanitized opener and before/after container metadata. Click alone is
insufficient. Require explicit provider identity or stable DOM binding; conflicts/ambiguity stop.
No operational extraction, normalization changes, identity-gate weakening, automatic activation or live run.

LATEST: normalization proven; diagnostic 02 stopped on 1755 detail identity. Do not revisit normalization.
Read docs/ASCEND_DETAIL_IDENTITY.md. New identity-only owner-ascend-detail-identity-1755-20260911-01
prepared, never run automatically. Max one detail open; no operational fields or remaining loads.
Persist sanitized identity evidence only; no automatic contract activation or other-system networking.

LATEST: board-normalization diagnostic 02 prepared, owner execution only; see ASCEND_BOARD_NORMALIZATION.md.
Diagnostic 01 saw 1 row/1 pickup/0 deliveries, no details. Do not guess retained filter cause or change
detail extraction. Fixed DataTables presentation resets only; exact IDs/8-3 plus semantic board hash required.
Unknown active filters fail closed. No live networking here; original multi-system phases remain untouched.

LATEST: isolated Ascend diagnostic owner-multi-system-ascend-diagnostic-20260911-01 prepared only.
Read docs/ASCEND_MULTI_SYSTEM_DIAGNOSTIC.md. Owner executes; do not launch here. Separate diagnostic
DB/grant, metadata-only checkpoints after each stage/load, exact 8/3 plus final board hash, max 11
detail opens. Original multi-system grants untouched; no CV/Outlook/report phase advancement.

LATEST: multi-system owner-multi-system-20260911-01 prepared only; read docs/POC_002_MULTI_SYSTEM.md.
No live vendor networking here. Owner executes staged Ascend exact 11 Active Loads details, tenant
CarrierView active/future (14 GET max, no past), narrowly eligible Outlook gaps (9 GET max), local report.
7/1/3 is oracle after classification only. Mail claims unverified; no writes/creation/communications.
Missing/unknown fields stay unknown; bounded misses are not absolute absence. Pilot is owner-review only.

LATEST: owner-table-schema-001 succeeded; observed 33-column grid contract now implemented offline.
login-discover-ops owner-live-ops-002 (unused) reads matched board rows only, no details. Reject header
clones, drift and multiple data grids; IDs come from same-row Load ID. Require explicit pagination proof
for COMPLETE_CURRENT_BOARD; no system-coverage claim. Owner executes next command; no live action here.
Prior schema-UNKNOWN/table-diagnostic-next statements below are historical. See ASCEND_ALL_LOADS_TABLE.md.

LATEST: owner-live-ops-001 consumed, stopped ops_board_headers_missing_or_ambiguous after board access.
Do not rerun parser unchanged. Prepared login-diagnose-table owner-table-schema-001 is OWNER-EXECUTED
only, offline tests here. Read docs/ASCEND_ALL_LOADS_TABLE.md. Fixed 2026-09-11 table/schema diagnostic,
same local Python/msedge/profile/normal networking; no details, manifests, pagination clicks or writes.
Tenant OWNER_ATTESTED; actual schema UNKNOWN until observed; no new LIVE_VALIDATED promotion.

LATEST PRIORITY: POC #002 — LIVE OPERATIONS supersedes historical 1752 validation. Owner reports 1760
delivered and expects ~8 pickups/~3 deliveries tomorrow; do not invent IDs/date/rows or force counts.
Prepared login-discover-ops prompts explicit operating date and board date format, uses the same headed
Python/msedge profile, normal app networking and fixed read-only operations. Owner executes the command
at the first live boundary; do not start it here. Keep bootstrap -02 and prior historical attempts untouched.
Manifest covers only observed visible board with explicit partial coverage. Provider facts, owner reports
and derived classifications stay separate. Unknown phones/dates/stop counts/zones/mappings remain unknown.
Bounded CarrierView tenant GET helper is prepared for discovered identities only; no agent fallback and
no mutation. Unverified list misses do not establish absence. Separate runtime operations store/proposal
ledger has no dispatch or canonical mutation. Pilot needs complete facts, fresh duplicate exclusion,
company wire mapping and documented/disclosed creation communication effects before explicit owner approval.
No live POC002 capability is validated; no pricing/Ascend/email/SMS/chat/write/batch authorization yet.

Latest owner override: same-process login-validate-1752 uses normal application networking, service
workers allowed, no method/origin/websocket filters and no cookie/network-body instrumentation. Safety
is the fixed read executor/action boundary, never a blanket HTTP restriction. Existing mutation blocks
remain. Add observed 1752 field proposals from actual DOM; no prior selector file is required. Unknown
or conflicting mappings remain UNKNOWN, no general-schema or LIVE_VALIDATED promotion. Run offline
tests only; owner executes prepared owner-same-process-1752-01. Do not consume bootstrap -02 or start a
live run here. Older conflicting same-process networking/missing-contract instructions are historical.

Latest instruction: OFFLINE TESTS ONLY this turn; no new live Ascend action and do not consume -02.
Continuity and same-process commands are implemented but NOT production-run. See ASCEND_ACCESS.md.
login-validate-1752 awaits future owner login/confirmation, retains the exact headed Python/msedge
page/context, then uses unchanged session/read/policy/write gates. No cross-process reuse prerequisite.
diagnose-continuity aggregates before/after storage and resource metadata only; no names/keys/values,
HTML, API payloads or raw errors. Same launch config; login-versus-read networking differs, and fresh
tab replacement can lose sessionStorage. Both are hypotheses, not a proven cause. Live field contract
still unverified. No new M4B LIVE_VALIDATED capability; owner identity remains OWNER_ATTESTED.

Latest owner instruction: DO NOT run validate-1752 / consume attempt -02 until the structural diagnostic
gap is resolved. Separately authorized diagnose-session completed: 15.011-second bounded render, all
frames inspected; root /, AscendTMS, complete, children 8, text length 23, one frame, zero nav/route
markers and no login form. SESSION_AUTHENTICATED remains unestablished; no load read or grant consumed.
Use only existing local Python/msedge profile. No raw DOM/text, unrelated URLs/query strings, traffic
inspection, secrets or screenshots. Keep diagnostic recommendation separate from load-validation gates.

Current M4B owner instruction (2026-09-10) supersedes older bootstrap identity/origin restrictions:
use only local Python Playwright/AveryBrowserSession/msedge and the existing dedicated Ascend profile.
No Computer Use/cloud/alternate context. Origin https://ascendtms.com is verified; never ask another
hostname or login yet. Tenant Booking Logistics is explicitly OWNER_ATTESTED, not provider DOM identity.
Authenticate separately with no login path/form plus Dashboard, Loads and two other approved visible
nav controls in one same-origin frame. No literal company/user requirement in this bootstrap branch.
One-use validate-1752 command may advance only after session/attestation/policy/local-evidence gates.
Runtime diagnostics contain only path, sanitized title, approved marker names, login presence and session
result. Never save raw HTML/cookies/tokens/notes/customer values/screenshots in diagnostic evidence.
Attempt owner-bootstrap-1752-20260910-01 consumed: root /, AscendTMS title, no login form, no nav markers;
stopped before /loads. No automatic retry, load read or M4B capability promotion. Provider tenant identity
remains unverified. See docs/ASCEND_ACCESS.md; earlier proposed-load/login instructions are historical.


M4B offline foundation is implemented/tested; read docs/ASCEND_ACCESS.md and docs/ASCEND_CONTRACT.md.
STOP before real Ascend login/navigation/read/write until separate owner authorization. No M4B
capability is LIVE_VALIDATED. Proposed first load is 1752, not automatically selected. Live origin,
account identity and DOM need authorized observation; never promote fixture selectors as verified.
Dedicated profile: C:\FreightDeskRuntime\Browser\booking-logistics\ascend, never the owner's profile.
Use one-use exact-load read grants. Production mutations remain independently blocked. Synthetic
write/session tests are not vendor evidence. No automatic retry after uncertainty.

M3 offline implementation is complete. Read docs/OUTLOOK_ACCESS.md before any Microsoft work.
Owner confirms bounded M3 production validation: OAuth, exact mailbox identity, settings read,
5-message discovery/read and ingestion/processing, and exactly 1 unsent draft manually verified in Outlook.
Only those six capabilities are LIVE_VALIDATED (see LIVE_POC.md). No attachments/delta/correlation/send
or other operational capability is validated. Do not run further Graph requests or helpers without
explicit authorization. Preserve the completed draft ledger; never recreate the draft automatically.
Entra provisions User.Read, Mail.ReadWrite, MailboxSettings.Read and Mail.Send. Current OAuth requests
only User.Read + Mail.ReadWrite + MailboxSettings.Read, never Mail.Send/.default/extra consent scopes.
Provisioning, OAuth request, ActionPolicy and LIVE_VALIDATED are independent states. Sending remains
APPROVAL_REQUIRED and all send HTTP execution blocked regardless of Entra/token provisioning.
Read selected mailbox preferences only; retain raw timezone/locale as context, not shipment time proof.
Do not request/expose tenant or client IDs in chat/logs; private local helper input only. No password/client secret.
Graph mail facts remain reviewable unverified proposals. Owner email identity requires bound trusted
proof, not display name or From. Never silently apply a message/attachment classification to live gates.
M4A owner approved the mapping, USD and all raw-data limitations for the 694-row source hash
af7e479a94f44f7f7541e2ecdd725de48e37656f2a0da92085670e16d0886bb0. Historical-only commit is complete.
Use ascend_commit with its source/batch/mapping-bound approval receipt; re-import is idempotent.
Changed versions require separate reconciliation; never overwrite committed evidence or use generic field assumptions.
Total Expenses is historical_total_expenses, never carrier pay without separate evidence. MC/DOT remain
strings; raw customer/equipment aliases remain unmerged. Exchange Rate Date is raw-only metadata.
No Ascend automation, live canonical/mail proposal/scheduler writes or further vendor networking.

CarrierView current owner configuration: tenant is the explicit API service credential;
agent employee token is unavailable for real API execution. Never retry/fallback between classes.
Use scripts/with-carrierview-token.ps1 (tenant default) and retain tenant credential-class audit,
actor FreightDesk/Avery, and the owner's recorded manager/admin eligibility reason.
Owner reconciled completed Booking reference 1752 and approved historical canonical import.
LIVE POC #001 is imported and displayed in a protected session. Only its scoped capability matrix is
LIVE_VALIDATED. History is partial; no operational monitoring/actions. Load 1753 remains separate/unread.
No vendor production write is authorized. The approved M4A local historical commit is separate.
Existing agent-first instructions in older handoffs are historical.

Work only inside the selected project unless the owner explicitly authorizes outside access.
Do not inspect unrelated folders, browser profiles, credentials or other repositories.
The request attachments and C:\FreightDeskRuntime are explicitly authorized; this does not authorize unrelated files.

Read CURRENT_STATE.md and KNOWN_ISSUES.md at session start. Proceed autonomously on reversible
local code/tests/docs. Stop for real authentication, production secrets, manual login,
unauthorized production writes, irreversible/material risks or unresolved important business rules.
Never ask the owner to paste secrets into conversation.

FreightDesk owns rules, canonical state, policy, approval, audit, scheduling and tenant isolation.
Vendor adapters and model/browser harnesses execute bounded jobs. Never invent endpoints or
claim supported APIs without official evidence. No Ascend API or live vendor capability is verified.

Use typed Python and small deterministic modules, explicit transitions and dependency injection.
Every action honors role/tenant/pause/policy/verified-fact gates. LLM output is untrusted.
Do not bother healthy drivers. ALLOW must work without a human click.
Keep non-demo startup disabled until real identity/credential boundaries are deliberately implemented.
Local bootstrap must never become production auth.

Keep secrets/runtime data ignored. OneDrive sync is independent of Git ignore.
Never log raw commands/provider errors/secrets. New test temp files stay under C:\FreightDeskRuntime\Data\TestRuns.
All live runtime files, databases, documents, browser state, tokens and logs stay under the approved
nonsynced runtime root, never the OneDrive source. Use RuntimePaths and reject reparse points.
Use .tools/python/python.exe and scripts/test.ps1. Dependency warnings are documented.

Before ending milestones run relevant checks and exercise the dashboard when applicable.
Update CURRENT_STATE.md, KNOWN_ISSUES.md and DECISIONS.md; put only actual live evidence in LIVE_POC.md.
Report IMPLEMENTED, TESTED and LIVE_VALIDATED separately. Mock/UI tests do not validate vendors.

CarrierView ledger is implemented and fixture-tested; production writes remain blocked.
Never blindly retry uncertain writes or silently elevate credentials. Before live reads, require owner
token confirmation plus verified official origin/response contract. Reconcile exact known-load facts
against the UI before LIVE_VALIDATED. Local webhook events stay quarantined until wire/auth verification.
